main.employees  [table]
───────────────────────

COLUMNS
  emp_id  INTEGER  [PK]
  full_name  TEXT  [NN]
  email  TEXT
  dept_id  INTEGER  [FK→main.departments]  [NN]
  salary  REAL  [NN]  DEFAULT 0.0
  hire_date  TEXT

CONSTRAINTS
  [FK]  fk_employees_0  (dept_id → main.departments)
  [UNIQUE]  idx_emp_email  (email)
  [PK]  pk_employees  (emp_id)

INDEXES
  idx_emp_active_dept  (dept_id)
  idx_emp_dept  (dept_id, hire_date)
  idx_emp_email  UNIQUE (email)
  idx_emp_email_lower  ((expr))

TRIGGERS
  trg_emp_after_delete  AFTER DELETE
  trg_emp_after_insert  AFTER INSERT
  trg_emp_before_insert  BEFORE INSERT
  trg_emp_before_update  BEFORE UPDATE
  trg_emp_salary_update  BEFORE UPDATE

  depends_on
    in:
      ← main.active_departments  [view]
      ← main.employee_summary  [view]

  fires_on
    in:
      ← main.trg_emp_after_delete  [trigger]
      ← main.trg_emp_after_insert  [trigger]
      ← main.trg_emp_before_insert  [trigger]
      ← main.trg_emp_before_update  [trigger]
      ← main.trg_emp_salary_update  [trigger]

  has_column
    out:
      → main.employees.dept_id  [column]
      → main.employees.email  [column]
      → main.employees.emp_id  [column]
      → main.employees.full_name  [column]
      → main.employees.hire_date  [column]
      → main.employees.salary  [column]

  has_constraint
    out:
      → main.employees.fk_employees_0  [constraint]
      → main.employees.idx_emp_email  [constraint]
      → main.employees.pk_employees  [constraint]

  has_index
    out:
      → main.employees.idx_emp_active_dept  [index]
      → main.employees.idx_emp_dept  [index]
      → main.employees.idx_emp_email  [index]
      → main.employees.idx_emp_email_lower  [index]

  references
    out:
      → main.departments  [table]
    in:
      ← main.assignments  [table]

  Details
  bodyHash  (none)
  level     full

