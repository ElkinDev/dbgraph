# Tasks: Phase 4 — CLI & Config

Standing header (every task): STRICT TDD (red→green; write the failing test FIRST, then code). Pure units
(`parseArgv`, config parse/resolve/build/write, incremental selector, diff engine, formatters, exit-map) use inline
fixtures / FAKE `GraphStore` doubles — NO DB. Integration is gated by `skipIf(!process.env.DBGRAPH_INTEGRATION)`;
SQLite E2E uses a COMMITTED torture fixture (no Docker), MSSQL E2E uses Testcontainers (reuses Phase-3 harness, NEVER
touches a real or named validation database). ZERO new runtime deps — hand-rolled parser + `node:readline`. Honor: hexagonal
ADR-004 (`src/cli` imports ONLY `src/index.ts` + `node:*`, NEVER `src/adapters/**`; `src/core/**` imports nothing
outward), determinism ADR-008 (pure golden-pinned formatters), no `any`, `exactOptionalPropertyTypes`. Read-only
against TARGET DB is INVIOLABLE. Conventional commits with US IDs; English. Record gotchas in `docs/learnings.md`.

> DESIGN OVERRIDE (orchestrator, authoritative): the SHARED `explore` formatter lives in `src/core/present/`
> (PURE, core-types-only, reused by Phase-5 MCP) — NOT in `src/cli/format/` as design Decision 2 stated. CLI-only
> formatters (`query`/`status`/`diff`) stay in `src/cli/format/`. `dbgraph.config.json` lives at PROJECT ROOT;
> `.dbgraph/` stays fully gitignored.

## Phase 1: Core errors + barrel seams + config layer (Batch A)

- [x] 1.1 RED→GREEN add `ConfigError` (`E_CONFIG`) and `UnsupportedDialectError` (`E_UNSUPPORTED_DIALECT`, message lists available dialects) to `src/core/errors.ts`; both extend `DbgraphError`, actionable messages, no `any`. Spec: cli-config "ConfigError and UnsupportedDialectError join the typed error set" (scenario: typed errors exist/exported). BLOCKING — gates all of Batch A/B. Done: `npm test errors` asserts both exist with codes; `ConnectionError`/`PermissionError` unchanged.
- [x] 1.2 Re-export the two errors via `src/core/index.ts` (alongside existing error exports) and confirm reachable through `src/index.ts`. Spec: same requirement (exported via barrels). Done: `npx tsc --noEmit`; import from `@niklerk23/dbgraph` resolves both.
- [x] 1.3 Add `capabilitiesFor(dialect): CapabilityMatrix` to `src/index.ts` (composition root may import engine `SQLITE_CAPABILITIES`/`MSSQL_CAPABILITIES`); unknown dialect throws `UnsupportedDialectError`. Spec: cli-config "Interactive init is capability-driven" (wizard reads matrix without connecting/importing adapters); Decision 6. Done: `npm test` unit asserts `capabilitiesFor('sqlite').supported` lacks `procedure`; `capabilitiesFor('x')` throws `UnsupportedDialectError`.
- [x] 1.4 RED→GREEN `src/cli/config/schema.ts` + `parse-config.ts`: define `DbgraphConfig` (dialect discriminant, `source`, optional `levels`, optional `include`/`exclude`, optional mongo sampling), and `parseConfig(json): DbgraphConfig` validating shape. Spec: cli-config "Config model is a dialect-discriminated, env-only schema" (valid parses; unknown dialect → `UnsupportedDialectError`; malformed level/missing field → `ConfigError` naming field). Done: `npm test parse-config` covers all 3 scenarios.
- [x] 1.5 RED→GREEN `src/cli/config/resolve-secrets.ts`: `resolveSecrets(cfg)` expands `${env:VAR}` from `process.env`; unset var → `ConfigError` naming the variable (never empty); resolved URL NEVER returned to any logger. Spec: cli-config "Plaintext credentials are rejected, env refs resolved at runtime" (env resolves; missing var fails loudly). Done: `npm test resolve-secrets` (set→value, unset→`ConfigError` naming var).
- [x] 1.6 RED→GREEN `src/cli/config/build-config.ts` + `write-config.ts`: ONE pure `buildConfig(inputs): DbgraphConfig` (used by BOTH flag form and wizard) + `writeConfig` (`JSON.stringify(cfg,null,2)+'\n'`, FIXED key order). Any identity field (host/port/database/user/domain/password/url) that is not a `${env:VAR}` token → `ConfigError` instructing `${env:VAR}`. Spec: cli-config "Plaintext credentials are rejected" (inline plaintext rejected) + Decision 5. Done: `npm test build-config` (literal host/user/password each throw `ConfigError`).

## Phase 2: Hand-rolled parser + dispatch + exit-code contract (Batch B)

- [x] 2.1 RED→GREEN `src/cli/parse/args.ts`: pure `parseArgv(argv): { command, positionals, flags }` — supports `--flag value`, `--flag=value`, booleans `--json`/`--full`/`--last`, short `-i`. Spec: cli-config command surface (parser feeds all six commands). Done: `npm test args` (inline argv arrays → expected structs incl. `=` form and booleans).
- [x] 2.2 RED→GREEN `src/cli/dispatch.ts`: pure command→handler table; unknown command is reported (no throw of raw Error). Spec: cli-config "CLI exit codes are a stable contract" (unknown command path). Done: `npm test dispatch` (known maps to handler; unknown flagged for usage+exit 2).
- [x] 2.3 RED→GREEN exit-code mapper in `src/cli/exit-code.ts` (PURE mapper extracted: `exitCodeFor(outcome)`): 0 success; 1 query zero-hits / diff-has-changes; 2 `ConnectionError` (message distinguishes DNS-not-resolved / refused / timeout) + unknown command; 3 `PermissionError` (names exact permission); 4 `UnsupportedDialectError` (lists dialects); other `DbgraphError`/`ConfigError` → non-zero. Spec: cli-config "CLI exit codes are a stable contract" (all 3 scenarios). Done: `npm test exit-code` (each error/outcome → expected code, pure).
- [x] 2.4 Build `src/cli/cli.ts` shebang entry: parse → dispatch → run handler → map thrown error/return to exit code → `process.exit`. Handlers THROW/RETURN, never call `process.exit` (keeps them unit-testable). Add `--help`/usage text. Spec: cli-config exit contract + Decision 9. Done: `npx tsc --noEmit`; `node dist/cli.js bogus` exits 2 with usage (covered later by 7.4 E2E).

## Phase 3: init + wizard + .gitignore writer (Batch C)

- [x] 3.1 RED→GREEN `src/cli/init/wizard.ts`: `node:readline` wizard offering ONLY object types in `capabilitiesFor(dialect).supported`; connection step REJECTS a literal credential and re-prompts for `${env:VAR}`; records ENV VAR NAME, never the secret value; secret prompts use readline with terminal:false + output:null-equivalent (async iterator pattern, no echo) — ZERO new deps. Spec: cli-config "Interactive init is capability-driven and byte-identical" (offers only supported types; rejects literal credential). Done: `npm test wizard` (17 tests: fed SQLite matrix → no `procedure` offered; literal password → re-prompt; secret not echoed to output).
- [x] 3.2 RED→GREEN `src/cli/commands/init.ts`: both paths (flag form and `-i`) feed ONE `buildConfig` → `writeConfig` at PROJECT ROOT `dbgraph.config.json` (`${env:VAR}` only, no secrets), append `.dbgraph/` to `.gitignore` (idempotent), then invoke sync seam; returns success (exit 0 mapped in `cli.ts`). Spec: cli-config "init writes a root config and gitignores the local index" (non-interactive writes root config+gitignore+sync; never writes plaintext). Done: `npm test init` (13 tests: writes root config with `${env:VAR}` refs; `.dbgraph/` appended once; literal flag value rejected with ConfigError before file written; Batch D sync seam wired).
- [x] 3.3 RED→GREEN byte-identity golden: flag-form inputs and equivalent wizard inputs produce IDENTICAL `writeConfig` string. Both paths funnel through the same `buildConfig`/`writeConfig`. Spec: cli-config "Interactive init … byte-identical to flags" (scenario: interactive and flag forms emit byte-identical config, ADR-008). Done: `npm test init` asserts byte-identical strings and the golden pinned.

## Phase 4: sync (incremental) + status (Batch D)

- [x] 4.1 RED→GREEN `src/cli/sync/incremental.ts`: pure selector over a FAKE in-memory `GraphStore` double — given current `getNodesByKind` rows + freshly extracted objects keyed by `body_hash`, returns exact id sets to `deleteNodes` (removed) and `upsertGraph` (new/changed). Spec: cli-config "sync is incremental by fingerprint, --full forces a rebuild" (changed source applies only the delta). Done: `npm test incremental` (asserts exact delete/upsert id sets; unchanged → empty delta).
- [x] 4.2 RED→GREEN `src/cli/commands/sync.ts`: resolve config → `createSqliteSchemaAdapter`/`createMssqlSchemaAdapter` (via `src/index.ts`) → compare `fingerprint()` vs last snapshot fingerprint → SKIP extraction when equal; else `extract`→`normalizeCatalog`→apply delta (4.1)→`putSnapshot` (per-type counts). `--full` forces full re-extract regardless of fingerprint. Spec: cli-config sync requirement (unchanged fingerprint skips; `--full` rebuilds; snapshot recorded). Done: `npm test sync` against fake adapter+store (equal fingerprint → no extract; `--full` → extract called). Also filled `syncAfterInit` seam in `init.ts`; `dispatch.ts` wired with real `handleSync`/`handleStatus`.
- [x] 4.3 RED→GREEN `src/cli/format/status.ts` (CLI-only pure formatter) + `src/cli/commands/status.ts`: gather `getNodesByKind` counts + `listSnapshots` last snapshot (timestamp/engine/counts) + filtered-out count + drift (live `fingerprint()` ≠ stored). Spec: cli-config "status reports counts, last snapshot and live drift". Done: `npm test status` (formatter golden shows counts+snapshot+DRIFT+excluded count for a fixed input).

## Phase 5: shared explore formatter + query + explore (Batch E)

- [x] 5.1 RED→GREEN `src/core/present/explore.ts` (PURE, core-types-only; NO `process`/`Date.now()`/adapter imports): `formatExplore(view: ExploreView, detail: ExploreDetail): string`, `ExploreView = { node: GraphNode; neighbors: NeighborGroups }`, levels `brief|normal|full`. Re-export `formatExplore`/types via `src/core/index.ts` (and reachable through `src/index.ts`). Spec: cli-config "explore output comes from a pure formatter shared with the MCP tool" (pure fn of bundle+detail, no CLI-only branching) + Decision 1. Done: `npm test explore-format` (golden per detail level); existing `test/core/boundaries.test.ts` still green (core/present imports nothing outward).
- [x] 5.2 RED→GREEN `src/cli/commands/explore.ts`: resolve qname → node + `getNeighbors(store,{nodeId})` → assemble `ExploreView` → `formatExplore(view, detail)`; `--detail` defaults `normal`. Spec: cli-config explore requirement (renders bundle at requested detail; deterministic golden-pinned). Done: `npm test explore` (brief/normal/full produce the formatter output; re-run byte-identical).
- [x] 5.3 RED→GREEN `src/cli/format/query.ts` (CLI-only pure formatter, incl. `--json` STABLE serialization) + `src/cli/commands/query.ts`: `search(store,{term})` → print each hit type+qname; `--json` deterministic byte-identical; ZERO hits → signal exit 1 (mapped in `cli.ts`). Spec: cli-config "query is backed by core search with a stable JSON contract" (prints hits exit 0; `--json` stable; zero results exits 1). Done: `npm test query` (text+`--json` goldens; zero-hit outcome maps to exit 1).

## Phase 6: graph-storage v1→v2 delta + diff engine (Batch F)

- [x] 6.1 RED→GREEN `src/adapters/storage/sqlite/schema.ts`: add `SNAPSHOT_OBJECTS_DDL` — `CREATE TABLE IF NOT EXISTS snapshot_objects(snapshot_id, node_id, kind, qname, body_hash)` + `idx_snapshot_objects_snapshot`. Spec: graph-storage "Per-object snapshot manifest" (manifest carries those columns). Done: `npx tsc --noEmit`; DDL constant exported.
- [x] 6.2 RED→GREEN `src/adapters/storage/sqlite/migrations.ts`: APPEND `{ version: 2, up }` (runs `SNAPSHOT_OBJECTS_DDL`) and bump `CURRENT_SCHEMA_VERSION = 2`. Spec: graph-storage "Schema versioning and migrations" (current=2; current schema opens no-op; existing v1 opens with NO data loss) + Decision 1. Done: `npm test migrations` (fresh DB reaches v2; pre-seeded v1 DB auto-migrates, nodes/edges preserved, no `SchemaVersionError`; v2 DB opens no-op).
- [x] 6.3 RED→GREEN `src/adapters/storage/sqlite/sqlite-graph-store.ts`: `putSnapshot` ALSO fills `snapshot_objects` via `INSERT ... SELECT id,kind,qname,body_hash FROM nodes WHERE <visible> ORDER BY qname,id` INSIDE the snapshot transaction (no port signature change); add a manifest reader `getSnapshotObjects(snapshotId)`. Spec: graph-storage "Per-object snapshot manifest" (one row per object, retrievable by snapshot_id, local-index only — NO target write) + "Snapshot persistence" (deferral lifted) + Decision 3. Done: `npm test sqlite-graph-store` (one manifest row per visible node; rows retrievable by snapshot_id).
- [x] 6.4 RED→GREEN `src/cli/diff/engine.ts`: pure `diffManifests(a, b)` → `{ added, removed, changed }` grouped by kind (added/removed by node_id/qname; changed by differing `body_hash`). Spec: cli-config "diff compares snapshots per object" + graph-storage "Two manifests support a per-object diff". Done: `npm test diff-engine` (two manifest arrays → exact added/removed/changed sets).
- [x] 6.5 RED→GREEN `src/cli/format/diff.ts` (CLI-only pure formatter) + `src/cli/commands/diff.ts`: read two manifests (`<snapA> <snapB>` or `--last` = two most recent) via `getSnapshotObjects`; format grouped diff stating WHAT changed (body changed by hash); NO changes → exit 0, changes → exit 1 (mapped in `cli.ts`); pre-v2/manifest-less snapshot degrades gracefully ("re-sync to enable per-object diff"). Spec: cli-config diff requirement (groups added/removed/modified; `--last`; exit reflects changes) + Decision 1. Done: `npm test diff` (formatter golden; changed→exit 1 outcome, none→exit 0).

## Phase 7: build wiring + boundary test + E2E + zero-warning sweep (Batch G)

- [x] 7.1 Create `tsup.config.ts` (two entries: `index` esm+cjs+dts unchanged; `cli` = `src/cli/cli.ts`, ESM only, `banner.js='#!/usr/bin/env node'`) + `package.json` `"bin": { "dbgraph": "./dist/cli.js" }` and `"build": "tsup"`. Spec: cli-config "bin/build wiring" + Decision 8. Done: `tsup.config.ts` created with two entries; `package.json` updated with `bin` + `build: tsup`; `npx tsc --noEmit` clean.
- [x] 7.2 RED→GREEN `test/cli/boundaries.test.ts` (reuse `collectTsFiles`+`extractImportSpecifiers` from `test/core/boundaries.test.ts`): FAIL if any `src/cli/**` file imports a specifier containing `/adapters/` OR a forbidden driver; PASS for `../index.js`/`@niklerk23/dbgraph`/`node:*`. Spec: cli-config "CLI never imports an adapter directly (boundary enforced)" (fails on direct adapter import) + Decision 7. Done: 8 boundary tests pass (green for current tree; proven to fail on planted adapter import).
- [x] 7.3 Confirm `.gitignore` ignores `.dbgraph/` and that `dbgraph.config.json` is COMMITTABLE (not ignored); confirm repo write-verb scanner still green (no write SQL added against target). Spec: proposal Success Criteria (target strictly read-only; `.dbgraph/` gitignored). Done: `.dbgraph/` present in .gitignore; `dbgraph.config.json` not ignored; `npm test security-scan` 8/8 green.
- [x] 7.4 RED→GREEN `test/cli/e2e.test.ts` (committed SQLite torture fixture, real `.dbgraph/`, NO Docker): `init → sync → query/status/diff`; assert stdout goldens + exit codes; SECOND `sync` is a no-op (incremental); mutate a body → `diff --last` shows `changed` and exits 1; `query <miss>` exits 1; bogus command exits 2. Spec: cli-config exit contract + all command requirements (E2E). Done: 15 E2E tests pass.
- [x] 7.5 RED→GREEN `test/cli/mssql.e2e.integration.test.ts` (`skipIf(!process.env.DBGRAPH_INTEGRATION)`, reuse Phase-3 Testcontainers harness): `init → sync → query` against ephemeral SQL Server 2022; NEVER touches the the validation database DB. Spec: proposal Success Criteria (MSSQL init→sync→query green under `DBGRAPH_INTEGRATION=1`); US-001/005/020. Done: test file created (5 gated tests + skip placeholder); Docker daemon not running on dev machine — deferred to CI.
- [x] 7.6 Final gates + story updates: `npm test` 882/882 green, `npm run lint` 0 errors 0 warnings, `npx tsc --noEmit` clean. Extracted duplicated config-loading into `src/cli/config/open-connections.ts` (single source); all lint issues across Batches A–F fixed; MSSQL E2E deferred to CI.

## Apply Batch Grouping (one sub-agent session each)

- **Batch A** (1.1–1.6): core errors + barrel seams (`capabilitiesFor`) + config layer (parse/resolve/build/write) — pure, NO DB. 1.1 BLOCKS everything.
- **Batch B** (2.1–2.4): hand-rolled parser, dispatch, exit-code mapper, `cli.ts` skeleton — pure, NO DB.
- **Batch C** (3.1–3.3): `init` + `node:readline` wizard + `.gitignore` writer + byte-identity golden — pure (fs-temp).
- **Batch D** (4.1–4.3): `sync` incremental (fake store) + `status` + status formatter — pure, NO DB.
- **Batch E** (5.1–5.3): shared `src/core/present/explore.ts` formatter + `explore` + `query` (+ `--json`) — pure goldens.
- **Batch F** (6.1–6.5): storage v1→v2 (`snapshot_objects`, auto-migrate, manifest in-txn) + diff engine + `diff` — pure + sqlite unit.
- **Batch G** (7.1–7.6): build (`bin`+tsup), boundary test, gitignore/security confirm, SQLite E2E, gated MSSQL E2E, final sweep.

### Dependency bottlenecks

- 1.1 (`ConfigError`/`UnsupportedDialectError`) gates ALL of Batch A/B — must land first.
- 1.3 (`capabilitiesFor`) gates 3.1 (wizard reads the matrix); 1.4–1.6 (config) gate 3.2 (`init`).
- 2.3 (exit-code mapper) gates 7.4 (E2E asserts exit codes) and every command's exit behavior.
- 5.1 (shared `explore` formatter in `src/core/present/`) gates 5.2 AND is the Phase-5 MCP seam — extend the EXISTING core boundary test, do not weaken it.
- 6.1→6.2→6.3 (schema → migration → manifest write) gate 6.4/6.5 (diff reads the manifest) and the 7.4 cross-sync `diff` assertion.
- 7.1 (tsup `bin`/two entries) + 7.2 (boundary test) gate the 7.4/7.5 E2E running the built CLI; Batch G is the integration critical path.
