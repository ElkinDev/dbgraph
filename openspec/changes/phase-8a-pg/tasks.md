# Tasks: Phase 8a — PostgreSQL Schema Extraction Adapter

Standing header (every task): STRICT TDD (red→green; NEVER mock `pg`). Pure units (tokenizer-core, map, error-map,
capabilities, parse-config) use captured `pg_catalog` JSON row fixtures or string fixtures — NO DB. Live-catalog
contract is integration-first against `postgres:16` Testcontainers, gated by `DBGRAPH_INTEGRATION=1`. Mirror the
SQLite adapter SHAPE (thin class + a SINGLE duck-typed driver seam) — do NOT scaffold a `strategies/` tree (that
machinery is SQL-Server-only). NO new deps beyond the PRE-APPROVED optional `pg` (lazy `import()`) + the existing
`testcontainers` dev dep. Conventional commits with US IDs; English. Honor: hexagonal ADR-004 (core imports nothing
outward; `createPgSchemaAdapter` is the ONLY join point), lazy-import ADR-006, determinism ADR-008, no `any`,
`exactOptionalPropertyTypes` (L-002 conditional spread). Reuse `ConnectionError`/`PermissionError` — no new error
types. Record any gotcha in `docs/learnings.md`.

Per-batch GATE (the batch's exit criteria, ALL must pass before the next batch): `npx tsc --noEmit` clean ·
`npm run lint` 0 errors / 0 warnings · `npm test` green (incl. the write-verb scanner
`test/adapters/engines/security-scan.test.ts` over `engines/**`) · plus the batch-specific proof noted in each
section. The integration batch additionally runs `DBGRAPH_INTEGRATION=1 npm run test:integration` green against a
REAL `postgres:16` container.

## Batch 1: Extract shared tokenizer-core; refactor MSSQL; prove goldens byte-identical (NO PG code)

> Satisfies US-028a (shared body-tokenizer module) and `schema-extraction` Requirement "Body-tokenizer primitives
> factored into a shared module with no MSSQL behavior change". The ONLY observable result of this batch is the
> MSSQL `RawCatalog`/goldens staying BYTE-IDENTICAL while the primitives move to `_shared/`.

- [ ] 1.1 RED: extend `test/adapters/engines/mssql/tokenizer.test.ts` (or add `test/adapters/engines/_shared/tokenizer-core.test.ts`) to import `canonicalizeQName`, `classifyAccess`, `extractWriteTargets`, `WRITE_VERB_PATTERNS` from `src/adapters/engines/_shared/tokenizer-core.ts` (module does not exist yet → RED). Cover: bracket/`"quote"` stripping + lowercase; INSERT/UPDATE/DELETE/MERGE/TRUNCATE → write; SELECT/FROM/JOIN → read; schema-qualified AND simple-name match. Spec: Shared tokenizer-core is the single source of the primitives. Done: test file fails to resolve the new module.
- [ ] 1.2 GREEN: create `src/adapters/engines/_shared/tokenizer-core.ts` by MOVING the three PURE primitives out of `mssql/tokenizer.ts`: `canonicalizeQName`, `extractWriteTargets` (now EXPORTED — currently a local fn), `classifyAccess`, and the `WRITE_VERB_PATTERNS` constant. `classifyAccess` MUST accept the dialect canonicalizer as an injected param — signature `classifyAccess(targetQName: string, body: string, canon?: (s: string) => string)` defaulting `canon` to `canonicalizeQName` so existing MSSQL callers are unchanged. NO behavior change. Spec: each engine applies its own dialect quoting while reusing the shared primitives. Done: 1.1 green; `npx tsc --noEmit`.
- [ ] 1.3 GREEN: refactor `src/adapters/engines/mssql/tokenizer.ts` to RE-IMPORT the three primitives + `WRITE_VERB_PATTERNS` from `_shared/`. KEEP local in mssql: `hasDynamicSql` (`/\b(exec|sp_executesql)\b/i`) and `tokenizeModuleDeps`. Re-export the shared primitives from the mssql module IF existing callers import them from there (preserve the public surface). Spec: the MSSQL adapter imports them from `_shared/` rather than defining its own copies. Done: `npx tsc --noEmit`; no other source file changed.
- [ ] 1.4 GATE — prove byte-identical: run `npm test` (full unit suite incl. `tokenizer.test.ts`, `mssql-schema-adapter.test.ts`, and the `security-scan.test.ts`); then `DBGRAPH_INTEGRATION=1 npm run test:integration` to re-assert the MSSQL golden `test/fixtures/mssql/dumps/mssql-dump-golden.json` is unchanged. Spec: MSSQL goldens stay byte-identical after the refactor (ADR-008). Done: every prior MSSQL suite + golden passes UNCHANGED; `tsc`/lint clean. CHANGING any MSSQL golden in this batch is a STOP — it means behavior drifted; investigate, do not re-bless.

## Batch 2: Capabilities + config plumbing (no live connection)

> Satisfies US-028 (connectivity/config surface), `pg-extraction` Requirements "Truthful PostgreSQL CapabilityMatrix"
> and "Connectivity via host/port/database/user/password with optional ssl" (parse half), and `schema-extraction`
> Requirement "SchemaAdapter port lives in core" (union widening). The union is STRUCTURAL — `PgAdapterConfig` is
> distinguished by the `host` key (mssql=`server`, sqlite=`file`); add NO `dialect` tag and touch NO existing member.

- [ ] 2.1 RED→GREEN `test/adapters/engines/pg/capabilities.test.ts` + `src/adapters/engines/pg/capabilities.ts`: `PG_CAPABILITIES: CapabilityMatrix` with `engine: 'pg'`; `supported` = `new Set` of `schema, table, column, constraint, index, view, procedure, function, trigger, sequence` (verbatim `NodeKind` names from `src/core/model/node.ts`); `supportsBodies: true`; `supportsDependencyHints: false`; `defaultLevels` per `DEFAULT_LEVELS`/engine specialization. Assert the matrix DECLARES each supported type and that `collections`/`fields` are absent. Spec: Matrix declares the supported object types; Matrix reports supportsBodies true and supportsDependencyHints false. Done: `npm test capabilities`.
- [ ] 2.2 RED→GREEN widen `src/core/ports/schema-adapter.ts`: add `PgAdapterConfig` (`host: string`, `port?: number` default 5432, `database: string`, `user: string`, `password: string`, `ssl?: boolean`, `schema?: string`) and extend `export type SchemaAdapterConfig = SqliteAdapterConfig | MssqlAdapterConfig | PgAdapterConfig`. Re-export through `src/core/ports/index.ts` + `src/core/index.ts`. Spec: SchemaAdapterConfig union includes the pg variant without a shape change. Done: `npx tsc --noEmit`; existing sqlite/mssql members untouched.
- [ ] 2.3 Verify prior phases stay green after widening: existing SQLite + MSSQL factories and their tests compile and pass unchanged. Spec: no new method is added to the SchemaAdapter port (the port SHAPE is unchanged). Done: `npm test` (all prior suites pass).
- [ ] 2.4 RED→GREEN `src/infra/config/schema.ts`: add `'pg'` to `SUPPORTED_DIALECTS` (currently `['sqlite','mssql']`); add the `PgSource` shape (host/port?/database/user/password/ssl?/schema?) and the `pg` member of `DbgraphConfig`. Spec: SUPPORTED_DIALECTS includes 'pg'. Done: `npx tsc --noEmit`.
- [ ] 2.5 RED→GREEN parse-config: `test/infra/config/parse-config.test.ts` (extend) + `src/infra/config/parse-config.ts` — add `parsePgSource` + `case 'pg'`. MUST reject a literal `password` (require `${env:VAR}`, env-only); default `port` to 5432 when omitted; accept optional `ssl`/`schema`. Unknown dialect STILL throws `UnsupportedDialectError`. Spec: Password must be supplied by env reference, not a literal; Connects with explicit credentials and default port. Done: `npm test parse-config`.

## Batch 3: Driver seam + error-mapper + factory (lazy import, synthetic errors — NO DB)

> Satisfies `pg-extraction` Requirements "Missing privilege raises an actionable PermissionError",
> "Authentication failure raises an actionable ConnectionError", and "Missing pg driver names the install command".
> Collapse lazy-`import('pg')` + connect + fingerprint wiring directly into `factory.ts` (SQLite shape) — do NOT add
> any strategy registry.

- [ ] 3.1 GREEN `src/adapters/engines/pg/driver.ts`: `ClientLike = { query(sql: string): Promise<{ rows: Record<string, unknown>[] }>; end(): Promise<void> }` (maps `pg.Client`); `PgReadonlyDriver { query(sql): Promise<Record<string,unknown>[]>; close(): Promise<void> }` returning `result.rows`; `createPgReadonlyDriver(client: ClientLike): PgReadonlyDriver`. A SINGLE short-lived `Client` (NOT a Pool). NO top-level `pg` import anywhere (ADR-006). The adapter + map talk ONLY to this seam (ADR-004). Spec: read strategy/lifecycle. Done: `npx tsc --noEmit`.
- [ ] 3.2 RED→GREEN `test/adapters/engines/pg/error-mapper.test.ts` + `src/adapters/engines/pg/error-mapper.ts`: pure `mapPgError(cause): DbgraphError` reading `err.code` SQLSTATE (synthetic error objects, NO DB): `28P01`/`28000` → `ConnectionError` (auth — check user/password); `42501` (or `insufficient_privilege`) → `PermissionError` naming the privilege + `docs/permissions/pg.md`; `3D000` (bad db) + `08*` (connection) → `ConnectionError` (host/port/database unreachable); else → `ConnectionError` (actionable fallback). Spec: Missing catalog privilege yields a typed, actionable PermissionError; Authentication failure raises an actionable ConnectionError. Done: `npm test error-mapper`.
- [ ] 3.3 RED→GREEN `test/adapters/engines/pg/factory.test.ts` + `src/adapters/engines/pg/factory.ts`: `createPgSchemaAdapter(config: PgAdapterConfig, deps?)` — lazy `import('pg')` (ADR-006), build the connection config (resolve `${env:VAR}` password, default port 5432, pass `ssl`), `new Client(...)` + `client.connect()`, map connect failures via `mapPgError`, wrap the connected client in `createPgReadonlyDriver` → `PgSchemaAdapter`. A missing `pg` package (`MODULE_NOT_FOUND` from the dynamic import) MUST raise an error whose message contains the exact `npm i pg`. `createPgSchemaAdapter` is the ONLY join point. Spec: Absent pg driver names npm i pg. Done: `npm test factory` (missing-driver suite asserts `npm i pg`).

## Batch 4: Queries + map + PG tokenizer (pure logic over captured rows — NO DB)

> Satisfies `pg-extraction` Requirements for schemas, tables/columns (incl. identity & generated), PK/FK/unique/CHECK,
> indexes (partial/expression/included), views + materialized views, functions/procedures, triggers, sequences,
> comments, "Parsed reads_from and writes_to", "Trigger fires_on plus its function body effects", and "Dynamic SQL is
> flagged via plpgsql EXECUTE … trigger EXECUTE FUNCTION is NOT dynamic". US-028 / US-028b / US-007. Read-only by
> construction (US-031): every SELECT-only constant must survive the engines write-verb scanner.

- [ ] 4.1 GREEN `src/adapters/engines/pg/queries.ts`: SELECT-only catalog query constants over `pg_namespace`/`pg_class`/`pg_attribute`/`pg_attrdef` (+ `format_type`, `attidentity`, `attgenerated`), `pg_constraint` (+ `pg_get_constraintdef`, `conkey`/`confkey`), `pg_index` (+ `pg_get_indexdef`), `pg_class.relkind 'v'/'m'` (+ `pg_get_viewdef`, `pg_matviews`), `pg_proc` `prokind 'f'/'p'` (+ `pg_get_functiondef`), `pg_trigger` non-internal (+ `pg_get_triggerdef`, `tgtype`), `pg_sequence` (+ `pg_class`), `obj_description`/`col_description`. Non-system filter `nspname NOT IN ('pg_catalog','information_schema')` AND exclude `pg_toast`/`pg_temp_*`; optional `AND nspname = $schema`. Every query ends `ORDER BY` schema, object[, ordinal] (ADR-008). Plus `SQL_PG_FINGERPRINT`. Spec: Read-only by construction — catalog SELECTs only. Done: `npx tsc --noEmit`.
- [ ] 4.2 Capture `pg_catalog` JSON row fixtures under `test/fixtures/pg/rows/*.json` for every family (schemas→tables→columns→defaults→constraints→indexes→views/matviews→functions/procedures→triggers→sequences→comments). Include identity + generated column rows, a composite FK, a partial index, an expression index, an `INCLUDE` index, a matview row, a procedure (`prokind='p'`) row. Done: files exist, valid JSON.
- [ ] 4.3 RED→GREEN `test/adapters/engines/pg/tokenizer.test.ts` + `src/adapters/engines/pg/tokenizer.ts`: PG `hasDynamicSql` MUST strip/ignore the `EXECUTE\s+(FUNCTION|PROCEDURE)\b` trigger-DDL clause BEFORE testing for a bare statement-form `EXECUTE` (`EXECUTE 'sql'` / `EXECUTE format(...)`), so a naive `\bexecute\b` does NOT false-positive on every trigger. `tokenizePgBody` wires the `_shared/` primitives with a PG canonicalizer (double-quote stripping only, no brackets). Pin BOTH directions: a plpgsql body with `EXECUTE format(...)` → `true`; a body whose only `EXECUTE` is `EXECUTE FUNCTION audit_fn()` → `false`. Spec: plpgsql dynamic EXECUTE statement is flagged with no invented edges; Trigger EXECUTE FUNCTION clause is NOT flagged as dynamic SQL. Done: `npm test pg/tokenizer`.
- [ ] 4.4 RED→GREEN `test/adapters/engines/pg/map.test.ts` + `src/adapters/engines/pg/map.ts`: `*Row` shapes + `buildPgRawCatalog(input, scope)` → deterministic `RawCatalog` (`engine: 'pg'`). REUSE the MSSQL `KIND_RANK` + `compareObjects` sort; columns by `attnum`; constraints by name; FK/index columns by array ordinal (`conkey`/`confkey`); trigger events grouped by name with timing+events decoded from the `tgtype` bitmask (`fires_on` destination = the PARENT TABLE, not the trigger nor the executed function); `schemas` distinct-sorted. Materialized view → `{ kind: 'view', extra: { materialized: true } }` (NO new `NodeKind`). Body level-gating: `full`=body, `metadata`=no body, `off`=object absent. Edges from bodies via `tokenizePgBody` at `confidence: 'parsed'` (no `score`); identity vs generated columns preserved distinctly; `obj_description`/`col_description` → comment on the correct target. Spec: every per-object extraction Requirement above; Materialized view is emitted as a view flagged materialized. Done: `npm test pg/map`.
- [ ] 4.5 Confirm US-031 scanner covers the new PG SQL: run `test/adapters/engines/security-scan.test.ts` — the `queries.ts` SELECT strings pass with NO write-verb false-positive (write verbs appearing only inside comments/`pg_get_*` literals must not trip the scan). Spec: pg SQL passes the engines write-verb scanner (US-031). Done: `npm test security-scan`.

## Batch 5: Adapter class + dispatch wiring + pinned message (+ code-4 regression guard)

> Satisfies `pg-extraction` "fingerprint via one cheap catalog query" (US-009) and `schema-extraction` Requirement
> "Supported dialects, capabilitiesFor and UnsupportedDialectError recognize pg". CRITICAL: the pinned-error change is
> MESSAGE-ONLY. `exit-code.ts:55` maps `UnsupportedDialectError → 4` via `instanceof` (NOT the string), so `exit-code.ts`
> is UNTOUCHED — instead add an explicit `instanceof` code-4 regression-guard assertion. The message + its pinned
> assertion + the code-4 guard all land in THIS batch (the contract stays in sync).

- [ ] 5.1 GREEN `src/adapters/engines/pg/pg-schema-adapter.ts`: `PgSchemaAdapter` mirroring `MssqlSchemaAdapter` — `dialect`/`capabilities` getters; `extract(scope)` runs the catalog queries (parallel where independent) → `buildPgRawCatalog`, honoring levels; `fingerprint()` runs EXACTLY ONE query `SQL_PG_FINGERPRINT` (`SELECT MAX(...), COUNT(*)` over a DDL-advancing marker — `MAX` over `pg_class.oid`/`pg_attribute.attnum` + relation/attr `COUNT(*)` across non-system schemas) and returns `sha256(\`${marker}|${count}\`)`; idempotent `close()` (`_closed` flag); extract/fingerprint-after-close → `ConnectionError`. Spec: fingerprint one cheap query (US-009); read strategy/lifecycle. Done: `npx tsc --noEmit`. (Marker columns finalized + pinned by the golden in Batch 7.)
- [ ] 5.2 RED→GREEN `src/infra/open-connections.ts`: add `createPgSchemaAdapter` to the `AdapterAndStore` union and the dispatch branch (`case 'pg'`). Spec: composition-root wiring. Done: `npm test open-connections`.
- [ ] 5.3 RED→GREEN `src/index.ts`: add `case 'pg'` to `capabilitiesFor()` returning `PG_CAPABILITIES`; export `createPgSchemaAdapter` and `PG_CAPABILITIES` from the barrel (composition root, ADR-004). Spec: pg is a supported dialect and resolves its capabilities. Done: `npm test` (capabilitiesFor suite asserts `capabilitiesFor('pg') === PG_CAPABILITIES`).
- [ ] 5.4 RED→GREEN pinned message: change ONLY the `UnsupportedDialectError` message string in `src/core/errors.ts:108` to list `sqlite, mssql, pg`, and update its pinned assertion in the corresponding errors test in the SAME edit. Make NO change to `src/cli/exit-code.ts`. Spec: Pinned message and its assertion change together. Done: `npm test` (errors suite green with the new message).
- [ ] 5.5 RED→GREEN code-4 regression guard: add (or extend) the exit-code test to assert `exitCodeFor(new UnsupportedDialectError('xyz')) === 4` via the `instanceof` path — proving the message change did NOT perturb the mapping, WITHOUT editing `exit-code.ts`. Spec: UnsupportedDialectError lists pg and maps to exit code 4 (verified unchanged). Done: `npm test exit-code`.

## Batch 6: Permission doc + PermissionError doc-link + story reconciliation

> Satisfies `pg-extraction` Requirement "Minimal read-only role documented" (US-033). RECONCILE the story references
> that name `postgres.md` → `pg.md` (consistent with the `pg` dialect key).

- [ ] 6.1 GREEN `docs/permissions/pg.md`: a MINIMAL read-only role script — `CREATE ROLE … NOSUPERUSER LOGIN`, `GRANT CONNECT` on the database, `GRANT USAGE` on the target schema(s), and the catalog `SELECT` required to read object definitions; NO broad table-data read grants. State that this role suffices to extract the full torture schema (catalog metadata only). Spec: Permission doc ships the minimal read-only role script. Done: doc present and reviewable.
- [ ] 6.2 Verify the `PermissionError` from 3.2 names the privilege AND links to `docs/permissions/pg.md`; adjust the message if the path/text drifted. Spec: the message names the missing privilege and links to docs/permissions/pg.md. Done: `npm test error-mapper` (assertion includes `docs/permissions/pg.md`).
- [ ] 6.3 Reconcile story docs: in `docs/stories/05-adapters.md` (US-028) change `docs/permissions/postgres.md` → `docs/permissions/pg.md`; refine US-028 to PostgreSQL-only (matview-as-view + `extra.materialized`, body tokenizer `confidence: parsed` with `supportsDependencyHints: false`, `pg_depend` deferred) and ADD US-028a (shared tokenizer) + US-028b (matview without a model change). In `docs/stories/06-security.md` (US-033) add the `pg: docs/permissions/pg.md` checkmark alongside the existing mssql one. Record `phase-8b-mysql` as the pre-planned next change (zero carry-over). Spec: US-028 refined + US-028a/b added; permission-doc reference uses `pg.md`. Done: stories updated; no `postgres.md` literal remains.

## Batch 7: Torture fixture + Testcontainers E2E + CI + scanner sweep + lint (Docker-gated)

> Satisfies `pg-extraction` Requirements "Committed PostgreSQL torture fixture materialized via Testcontainers",
> "Golden-pinned RawCatalog and end-to-end pipeline", and the read-only/CI obligations. US-028. This is the
> Docker-dependent critical path; the gated job MUST NOT block the unit matrix.

- [ ] 7.1 GREEN `test/fixtures/pg/torture.sql` (reviewable plain-text, NO binary blob): a materialized view; a partial index AND an expression index (+ an `INCLUDE` index on PG11+); a `GENERATED ALWAYS AS (...) STORED` generated column; an IDENTITY column; a plpgsql function that writes TWO tables and reads a THIRD; a plpgsql function using a dynamic `EXECUTE` statement; an `AFTER UPDATE` trigger whose `EXECUTE FUNCTION` target writes an audit table (and a plain non-dynamic `audit_fn`); a sequence; a FK + a CHECK; `COMMENT ON` annotations on an object and a column. Exercises 100% of `PG_CAPABILITIES`. Spec: Fixture is a reviewable .sql script; Fixture exercises the required torture objects. Done: file parses (applied in 7.2).
- [ ] 7.2 GREEN `test/fixtures/pg/container.ts`: Testcontainers harness — image `postgres:16`, env `POSTGRES_PASSWORD`/`POSTGRES_DB`; wait-strategy = poll `SELECT 1` (NOT port-open), capped; apply `torture.sql`; expose `{ config, stop() }`; `pgContainerAvailable()` + `skipIf(!process.env.DBGRAPH_INTEGRATION)` with an explicit skip reason. Spec: Integration setup uses an ephemeral container and skips without DBGRAPH_INTEGRATION. Done: harness imports cleanly.
- [ ] 7.3 RED→GREEN `test/adapters/engines/pg/extract.integration.test.ts` (skipIf-gated): connect via `createPgSchemaAdapter` → `extract` → serialize via the existing `stableStringify` → seed `test/fixtures/pg/golden-raw-catalog.json`; a SECOND extraction on the same fixture is BYTE-IDENTICAL. This run FINALIZES + pins the exact `tgtype` decode and the `fingerprint` marker columns. Edge-endpoint assertions MUST pin BOTH source and destination qnames (L-009): the `INSERT`/`SELECT` function → `writes_to a`/`writes_to b`/`reads_from c`; the view → `reads_from` its base tables; the matview present as `kind:'view'` + `extra.materialized:true`; the trigger → `fires_on` the PARENT TABLE with `event=UPDATE`/timing `AFTER` plus `writes_to` the audit table from `audit_fn`; the dynamic-`EXECUTE` function `hasDynamicSql:true` with NO fabricated edges. Spec: RawCatalog golden is deterministic and byte-identical; Function/view/procedure parsed edges; Trigger fires_on + body effects. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.4 RED→GREEN fingerprint DDL/DML integration test (in the extract suite, gated): a DDL change (add a column/object) → `fingerprint()` value CHANGES; a data-only change (INSERT/UPDATE/DELETE) → value UNCHANGED; assert `fingerprint()` issues exactly ONE query and does NOT enumerate objects. Spec: fingerprint changes on DDL change; fingerprint is stable across data-only changes; one cheap query. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.5 RED→GREEN schema-scope + level integration assertions (gated): two user schemas → default extract contains both, none from `pg_catalog`/`information_schema`; `schema: 'app'` scopes to `app` only; a view at `metadata` present without body, a matview type `off` absent. Spec: All non-system schemas extracted by default; Configured schema scopes the extraction; Metadata level omits the view body, off level omits the object. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.6 RED→GREEN `test/adapters/engines/pg/e2e.integration.test.ts` (gated): `extract` → `normalizeCatalog` → `SqliteGraphStore.upsertGraph` → `impact`/`path` queries → seed `test/fixtures/pg/golden-e2e.json`; byte-identical re-run. Edge endpoints pinned (L-009). Spec: Full pipeline reaches impact and path queries golden-pinned. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.7 GREEN `package.json`: add `pg` to `optionalDependencies` (ADR-002/006 pre-approved; justify in commit body); `testcontainers` already present; `npm ci` succeeds. Spec: NO new runtime deps beyond the optional pg. Done: `pg` appears under `optionalDependencies`; `npm ci` clean.
- [ ] 7.8 GREEN `.github/workflows/ci.yml`: KEEP the existing `test` + `mssql-integration` jobs unchanged; ADD a separate gated `pg-integration` job (Linux-only, `needs: []`) setting `DBGRAPH_INTEGRATION=1`, running ONLY `npm run test:integration` (or the pg subset), using ONLY an ephemeral `postgres:16` container, never referencing any validation database, and never required by the unit matrix. Spec: pg CI job is gated and never touches a validation database. Done: YAML valid; unit matrix does NOT depend on `pg-integration`.
- [ ] 7.9 Final GATE + closeout: `npx tsc --noEmit` clean; `npm run lint` 0/0; `npm test` green incl. `security-scan.test.ts` over `engines/**` (catalog SELECTs only — no `SET SESSION READ ONLY`, no write verbs); `DBGRAPH_INTEGRATION=1 npm run test:integration` green against REAL `postgres:16`. Confirm 100% of `PG_CAPABILITIES` is exercised by the torture fixture. Record learnings in `docs/learnings.md`. Spec: write-verb scanner stays green; CI green including the gated pg-integration job. Done: all gates green.

## Apply Batch Grouping (one sub-agent session each)

- **Batch 1** (1.1–1.4): extract `_shared/tokenizer-core.ts`, refactor MSSQL to import it, prove MSSQL goldens byte-identical — NO PG code, NO DB beyond the gated MSSQL golden re-run.
- **Batch 2** (2.1–2.5): `PG_CAPABILITIES`, `PgAdapterConfig` union widening, `PgSource` config + parse-config `pg` branch — pure logic, NO connection.
- **Batch 3** (3.1–3.3): driver seam, error-mapper, factory (lazy `import('pg')`) — pure/synthetic, NO DB.
- **Batch 4** (4.1–4.5): queries, captured-row fixtures, PG tokenizer (`EXECUTE` vs `EXECUTE FUNCTION`), map, US-031 scanner confirm — pure logic, NO DB.
- **Batch 5** (5.1–5.5): adapter class + fingerprint, dispatch wiring (`open-connections`, barrel/`capabilitiesFor`), pinned message + code-4 regression guard.
- **Batch 6** (6.1–6.3): `docs/permissions/pg.md`, `PermissionError` doc-link verify, story reconciliation (`postgres.md`→`pg.md`, US-028/a/b).
- **Batch 7** (7.1–7.9): torture fixture, Testcontainers harness, RawCatalog golden + fingerprint DDL/DML + scope/level + E2E, `pg` optionalDependency, gated `pg-integration` CI, final scanner/lint/closeout — Docker-gated critical path.

### Dependency bottlenecks

- **Batch 1 gates EVERYTHING.** `_shared/tokenizer-core.ts` must land AND the MSSQL goldens must stay byte-identical (1.4) before any PG tokenizer (4.3) is built on the shared primitives. A golden drift here is a hard STOP.
- **2.2 (union widening) gates all PG modules** that reference `PgAdapterConfig` (3.3 factory, 5.1 adapter) and the prior-phase green check (2.3) must pass before wiring.
- **3.1 (driver seam) gates 3.3** (factory wraps it) and **5.1** (adapter talks only to it); **3.2 (error-mapper) gates 3.3** (factory maps connect errors) and **6.2** (doc-link).
- **4.1 queries + 4.4 map** must precede **5.1** (adapter wires them) and **7.3/7.6** (goldens assert their output).
- **5.4 message + 5.5 code-4 guard are atomic** — they MUST land in the same batch as one contract; splitting them risks a red pinned assertion or an unverified exit-code mapping.
- **7.1 torture.sql + 7.2 container gate ALL integration (7.3–7.6)** — the Docker-dependent critical path. The exact `tgtype` decode and `fingerprint` marker columns are FINALIZED by 7.3 against the live catalog, so 5.1's fingerprint is provisional until 7.3/7.4 pin it.
- **7.7 (`pg` optionalDependency) + the `test:integration` script** must be in place before 7.8's CI job and the 7.3–7.6 done-checks can run cleanly.

## Definition of Done (tied to the proposal's Success Criteria)

- [ ] `PgSchemaAdapter` implements the EXISTING `SchemaAdapter` port (no port SHAPE change), wired via `createPgSchemaAdapter` through the composition root (`open-connections.ts` + `src/index.ts`). — Batches 2, 3, 5
- [ ] Extracts the torture schema into a coherent `RawCatalog`: schemas; tables/columns/types/defaults/identity & generated; PK/FK/unique/CHECK; indexes (partial/expression/included); views; materialized views (`kind:'view'` + `extra.materialized:true`); functions; procedures; triggers (event+timing via `EXECUTE FUNCTION`); sequences; comments. — Batches 4, 7
- [ ] `reads_from`/`writes_to` classified from `pg_get_functiondef`/`pg_get_viewdef` bodies at `confidence:'parsed'`; plpgsql dynamic `EXECUTE` marks `hasDynamicSql:true`; `supportsDependencyHints:false` (body tokenizer only). — Batches 4, 7
- [ ] `PG_CAPABILITIES` truthful (schema/table/column/constraint/index/view/procedure/function/trigger/sequence; `supportsBodies:true`; `supportsDependencyHints:false`); 100% matrix coverage by the fixture. — Batches 2, 7
- [ ] `engines/_shared/tokenizer-core.ts` created and consumed by both MSSQL and PG; MSSQL goldens BYTE-IDENTICAL after the refactor. — Batch 1
- [ ] Connectivity via host/port/database/user/password (+ optional ssl); `password` is `${env:VAR}` env-only; `schema?` optional (default = all non-system schemas; set = scoped). — Batches 2, 3, 7
- [ ] `docs/permissions/pg.md` ships the minimal read-only role; a missing privilege raises an actionable `PermissionError` naming the privilege + doc; no `SET SESSION READ ONLY` workaround. — Batches 3, 6, 7
- [ ] `fingerprint()` returns a cheap one-query value that moves on DDL and is stable across data-only changes. — Batches 5, 7
- [ ] Determinism (ADR-008): the `RawCatalog` is golden-pinned and byte-identical in CI. — Batch 7
- [ ] The write-verb scanner over `src/adapters/engines/**` stays green (catalog SELECTs only). — Batches 4, 7
- [ ] NO new runtime deps beyond optional `pg`; `tsc`/lint/test clean; CI green including the gated `pg-integration` job (`DBGRAPH_INTEGRATION=1`), which never blocks the unit matrix. — Batch 7
- [ ] The pinned `UnsupportedDialectError` message updated to list `sqlite, mssql, pg` with its assertion AND the exit-code-4 mapping verified unchanged (via `instanceof` guard, `exit-code.ts` untouched). — Batch 5
- [ ] US-028 refined to PostgreSQL-only with US-028a/US-028b added to `docs/stories/05-adapters.md`; `phase-8b-mysql` recorded as the pre-planned next change (zero carry-over). — Batch 6
