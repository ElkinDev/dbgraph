# Tasks: Phase 8b — MySQL Schema Extraction Adapter

Standing header (every task): STRICT TDD (RED→GREEN→refactor; the failing test PRECEDES the code; NEVER mock
`mysql2`). Pure units (tokenizer, map, error-mapper, capabilities, parse-config, config-union) use captured
`information_schema` JSON row fixtures or string fixtures — NO DB. Live-catalog contract is integration-first
against a REAL `mysql:8` Testcontainers instance, gated by `DBGRAPH_INTEGRATION=1` (NEVER mock the driver).
Mirror the archived `pg/` adapter SHAPE file-for-file (thin class + a SINGLE duck-typed `MysqlReadonlyDriver`
seam) — do NOT scaffold a `mysql/strategies/` tree (that machinery is SQL-Server-only). `createMysqlSchemaAdapter`
is the ONLY join point (ADR-006, lazy `import('mysql2/promise' as string)`). NO new runtime dep beyond the
PRE-APPROVED optional `mysql2` + the existing `testcontainers` dev dep. Honor: hexagonal ADR-004 (core imports
nothing outward; downstream `normalizeCatalog → SqliteGraphStore → query` pipeline UNCHANGED), determinism
ADR-008, strict TS / no `any`, `exactOptionalPropertyTypes` (conditional spread). `path.win32`/`path.posix` — no
hard-coded separators. Reuse `ConnectionError`/`PermissionError` — no new error types. Conventional commits with
US-029; English. EXACT-set edge assertions from the start (`toEqual` sorted dep names, `toBe(N)` counts, explicit
no-self + no-phantom) — existence-only `find().toBeDefined()` is FORBIDDEN (it hid 8a CRITICAL-1).

Per-batch GATE (the batch's exit criteria, ALL must pass before the next batch): `npx tsc --noEmit` clean ·
`npm run lint` 0 errors / 0 warnings · `npm test` green INCLUDING the write-verb scanner
`test/adapters/engines/security-scan.test.ts` over `engines/**` · plus the batch-specific proof noted in each
section. Batch 1 additionally re-proves the pg goldens + pg tokenizer suite BYTE-IDENTICAL. Batch 7 additionally
runs `DBGRAPH_INTEGRATION=1 npm run test:integration` green against a REAL `mysql:8` container.

## Batch 1: PROMOTE `_shared/` tokenizer helpers; refactor PG; prove pg goldens byte-identical (NO MySQL code)

> Satisfies the `mysql-extraction` presence-gate prerequisite (D10) and the design's `_shared` PROMOTION decision.
> The ONLY observable result of this batch is the PG `RawCatalog`/goldens + pg tokenizer suite staying
> BYTE-IDENTICAL while `maskDynamicStrings` + `bodyContainsRef` move to `_shared/`. NO MySQL code in this batch.
> If a reviewer prefers duplication (D10 option b), ONLY this batch changes (drop the pg refactor; add the two
> functions to `mysql/tokenizer.ts` in Batch 4 instead) — Batches 2-7 are unaffected.

- [x] 1.1 RED: extend `test/adapters/engines/_shared/tokenizer-core.test.ts` to import `maskDynamicStrings` AND `bodyContainsRef` from `src/adapters/engines/_shared/tokenizer-core.ts` (not exported there yet → RED). Cover: single-quote literal masking with `''` escape (no `$$` touched); word-boundary presence TRUE for a referenced canonical qname, FALSE for an absent one and FALSE for a substring-only near-match. Done: test fails to resolve the new exports.
- [x] 1.2 GREEN: MOVE `maskDynamicStrings` (already exported) AND `bodyContainsRef` (currently PRIVATE in `pg/tokenizer.ts` — now EXPORT it) into `src/adapters/engines/_shared/tokenizer-core.ts`; keep its `escapeRegex` helper alongside. PURE, engine-agnostic, NO behavior change. Done: 1.1 green; `npx tsc --noEmit`.
- [x] 1.3 GREEN: refactor `src/adapters/engines/pg/tokenizer.ts` to IMPORT `maskDynamicStrings` + `bodyContainsRef` from `_shared/`; delete the pg local copies. Re-export from the pg module IF existing callers import them from there (preserve the public surface). Done: `npx tsc --noEmit`; no source file outside `_shared/` + `pg/tokenizer.ts` changed.
- [x] 1.4 Confirm `WRITE_VERB_PATTERNS` in `_shared/tokenizer-core.ts` covers `REPLACE` (a MySQL write verb). If MISSING: add it AND re-run the pg + mssql tokenizer suites + goldens to prove BYTE-IDENTICAL (a new write verb must not reclassify any existing pg/mssql edge). If already present: note it, no edit. Done: `REPLACE` matched; pg/mssql suites unchanged.
- [x] 1.5 GATE — prove byte-identical: `npm test` (full unit suite incl. `pg/tokenizer.test.ts`); then `DBGRAPH_INTEGRATION=1 npm run test:integration` to re-assert `test/fixtures/pg/golden-raw-catalog.json` + `golden-e2e.json` UNCHANGED. CHANGING any pg golden here is a STOP — behavior drifted; investigate, do NOT re-bless. Done: every pg suite + golden passes UNCHANGED; `tsc`/lint clean.

## Batch 2: Capabilities + config plumbing + the union JSDoc fix (no live connection)

> Satisfies `mysql-extraction` "Truthful MySQL CapabilityMatrix" + "Connectivity ... optional ssl" (parse half),
> and `schema-extraction` "SchemaAdapter port lives in core" + "Supported dialects ... recognize mysql"
> (SUPPORTED_DIALECTS half). The union is a STRUCTURAL union; dispatch keys on the EXPLICIT `dialect` field, NOT
> shape (pg and mysql BOTH have `host` — D11/Decision §7). Add NO `dialect` tag to union members.

- [x] 2.1 RED→GREEN `test/adapters/engines/mysql/capabilities.test.ts` + `src/adapters/engines/mysql/capabilities.ts`: `MYSQL_CAPABILITIES: CapabilityMatrix`, `engine: 'mysql'`; `supported = new Set` of `table, column, constraint, index, view, procedure, function, trigger` (verbatim `NodeKind` names) — NO `sequence`, NO standalone `schema`; `supportsBodies: true`; `supportsDependencyHints: false`; `defaultLevels = DEFAULT_LEVELS`. Assert the set EXCLUDES `sequence` and `schema`. Spec: Matrix declares the supported object types; Matrix reports no sequence and no standalone schema kind; Matrix reports supportsBodies true and supportsDependencyHints false. Done: `npm test capabilities`.
- [x] 2.2 RED→GREEN widen `src/core/ports/schema-adapter.ts`: add `MysqlAdapterConfig` (`host: string`, `port?: number` default 3306, `database: string`, `user: string`, `password: string`, `ssl?: boolean | { rejectUnauthorized?: boolean }`; NO `schema?`); extend `SchemaAdapterConfig` union. FIX the stale `PgAdapterConfig` "distinguished by `host`" JSDoc + add union JSDoc: the union is INTENTIONALLY non-discriminable by shape (pg and mysql both carry `host`); dispatch keys on the explicit config `dialect` field; each factory takes its concrete type; NO `dialect` discriminant added to members. Re-export through `src/core/ports/index.ts` + `src/core/index.ts`. Spec: SchemaAdapterConfig union includes the mysql variant without a shape change; The union is non-discriminable by shape and dispatch keys on the explicit dialect. Done: `npx tsc --noEmit`; sqlite/mssql/pg members untouched.
- [x] 2.3 Verify prior phases stay green after widening: existing SQLite/MSSQL/PG factories + their tests compile and pass unchanged. Spec: no new method is added to the SchemaAdapter port (the port SHAPE is unchanged). Done: `npm test` (all prior suites pass).
- [x] 2.4 RED→GREEN `src/infra/config/schema.ts`: add `'mysql'` to `SUPPORTED_DIALECTS`; add `MysqlSource` (host/port?/database/user/password/ssl? as strings parsed later; NO `schema`) + the `mysql` `DbgraphConfig` member. Spec: SUPPORTED_DIALECTS includes 'mysql'. Done: `npx tsc --noEmit`.
- [x] 2.5 RED→GREEN `test/infra/config/parse-config.test.ts` (extend) + `src/infra/config/parse-config.ts`: add `parseMysqlSource` + `case 'mysql'`. REQUIRE host/database/user/password; REJECT a literal `password` (reuse `isEnvRef`/`ENV_REF_RE` from `parsePgSource` — require `${env:VAR}`); default `port` to 3306; optional `ssl`; NO `schema` field. Unknown dialect STILL throws `UnsupportedDialectError`. Spec: Password must be supplied by env reference, not a literal; There is no schema config knob; Connects with explicit credentials and default port. Done: `npm test parse-config`.

## Batch 3: Driver seam + error-mapper + factory (lazy import, synthetic errors — NO DB)

> Satisfies `mysql-extraction` "Missing privilege raises an actionable PermissionError", "Authentication failure
> raises an actionable ConnectionError" (mapper half), and "Missing mysql2 driver names the install command".
> Collapse lazy-`import('mysql2/promise')` + connect + error mapping directly into `factory.ts` (pg/SQLite shape)
> — do NOT add any strategy registry. The seam normalizes `[rows, fields]` → `rows`.

- [x] 3.1 GREEN `src/adapters/engines/mysql/driver.ts`: `ConnectionLike = { query(sql: string, params?: unknown[]): Promise<[Record<string,unknown>[], unknown]>; end(): Promise<void> }` (duck-types `mysql2/promise`); `MysqlReadonlyDriver { query(sql, params?): Promise<Record<string,unknown>[]>; close(): Promise<void> }`; `createMysqlReadonlyDriver(conn)` destructures `const [rows] = await conn.query(...)` and returns `rows`. A SINGLE short-lived connection (NOT a pool); `close()` calls `conn.end()`. NO top-level `mysql2` import (ADR-006). Spec: read strategy/lifecycle. Done: `npx tsc --noEmit`.
- [x] 3.2 RED→GREEN `test/adapters/engines/mysql/error-mapper.test.ts` + `src/adapters/engines/mysql/error-mapper.ts`: pure `mapMysqlError(cause)` reading `err.errno` (number) THEN falling back to `err.code` (string) — synthetic `{ errno, code, message }` objects, NO DB. `1045` → `ConnectionError` (auth — check user/password); `1049` → `ConnectionError` (database not found); `code ECONNREFUSED`/`ETIMEDOUT`/`ENOTFOUND` + `1130` → `ConnectionError` (host unreachable); `1044`/`1142`/`1143`/`1370` → `PermissionError` naming the privilege + `docs/permissions/mysql.md`; else → `ConnectionError` (actionable fallback incl. `cause.message`). Spec: Missing catalog privilege yields a typed, actionable PermissionError; Authentication failure raises an actionable ConnectionError. Done: `npm test error-mapper`.
- [x] 3.3 RED→GREEN `test/adapters/engines/mysql/factory.test.ts` + `src/adapters/engines/mysql/factory.ts`: `createMysqlSchemaAdapter(config, deps?)` — lazy `import('mysql2/promise' as string)`, build conn config (resolve `${env:VAR}` password, default port 3306, pass `ssl`), `createConnection().connect()` (or `createConnection` awaited per `mysql2/promise`), map connect failures via `mapMysqlError`, wrap in `createMysqlReadonlyDriver` → `MysqlSchemaAdapter`. A `MODULE_NOT_FOUND` from the dynamic import MUST raise an error whose message contains the exact `npm i mysql2`. `createMysqlSchemaAdapter` is the ONLY join point. Spec: Absent mysql2 driver names npm i mysql2. Done: `npm test factory` (missing-driver suite asserts `npm i mysql2`).

## Batch 4: Queries + map + tokenizer + adapter class (pure logic over captured rows — NO DB)

> Satisfies `mysql-extraction` Requirements for the connected database namespace, tables/columns (incl.
> AUTO_INCREMENT + generated), PK/FK/unique/CHECK, indexes, views, functions/procedures, triggers, comments,
> "Parsed reads_from and writes_to ... no phantom or self edges", "Dynamic SQL via PREPARE/EXECUTE is flagged",
> and "fingerprint via one cheap catalog query". US-029 / US-007 / US-009. Read-only by construction (US-031):
> every SELECT-only constant must survive the write-verb scanner.

- [ ] 4.1 GREEN `src/adapters/engines/mysql/queries.ts`: SELECT-only `information_schema` constants — TABLES (`TABLE_TYPE='BASE TABLE'`, `TABLE_COMMENT`), COLUMNS (`COLUMN_TYPE`/`IS_NULLABLE`/`COLUMN_DEFAULT`/`EXTRA`/`GENERATION_EXPRESSION`/`COLUMN_COMMENT`), TABLE_CONSTRAINTS + KEY_COLUMN_USAGE + REFERENTIAL_CONSTRAINTS (PK/FK/unique; FK ref + ON UPDATE/DELETE; order by `ORDINAL_POSITION`; align ref by `POSITION_IN_UNIQUE_CONSTRAINT`), CHECK_CONSTRAINTS (`CHECK_CLAUSE`), STATISTICS (indexes; `SEQ_IN_INDEX`/`NON_UNIQUE`/`EXPRESSION`/`SUB_PART`; exclude `PRIMARY`), VIEWS (`VIEW_DEFINITION`), ROUTINES (`ROUTINE_TYPE IN ('PROCEDURE','FUNCTION')`, `ROUTINE_DEFINITION`/`ROUTINE_COMMENT`), TRIGGERS (`EVENT_MANIPULATION`/`ACTION_TIMING`/`EVENT_OBJECT_TABLE`/`ACTION_STATEMENT`). EVERY query `WHERE TABLE_SCHEMA = DATABASE()` (triggers/routines on their own `*_SCHEMA` column), EVERY query `ORDER BY` (ADR-008); no `?` params. Plus `SQL_MYSQL_FINGERPRINT` (one row: `table_count`/`column_count`/`routine_count` over `= DATABASE()`). Spec: Read-only by construction — catalog SELECTs only; fingerprint uses one cheap query. Done: `npx tsc --noEmit`.
- [ ] 4.2 Capture `information_schema` JSON row fixtures under `test/fixtures/mysql/rows/*.json` for every family (tables→columns→constraints→key-column-usage→referential→check→statistics→views→routines→triggers). Include an `auto_increment` `EXTRA` row, a `STORED GENERATED`/`VIRTUAL GENERATED` row, a composite FK (two `ORDINAL_POSITION` rows), a composite index (multi `SEQ_IN_INDEX`), an `EXPRESSION` (functional) index row, a `SUB_PART` (prefix) row, a CHECK row, a procedure + a function row, a trigger row. Done: files exist, valid JSON.
- [ ] 4.3 RED→GREEN `test/adapters/engines/mysql/tokenizer.test.ts` + `src/adapters/engines/mysql/tokenizer.ts`: `hasMysqlDynamicSql = /\b(prepare|execute)\b/i.test(body)` on the ORIGINAL body (no `EXECUTE FUNCTION` strip — MySQL triggers have no such clause). `mysqlCanonicalize` strips BACKTICKS (build the backtick char via `String.fromCharCode(96)` — no literal backtick in source) + (defensively) double-quotes, lowercases. `tokenizeMysqlBody(body, deps)`: `staticBody = maskDynamicStrings(body)`; for each dep skip unless `bodyContainsRef(staticBody, mysqlCanonicalize(qname))` (PRESENCE GATE — never default-to-read); then `classifyAccess(qname, staticBody, mysqlCanonicalize)`; push `{ target, access, confidence:'parsed' }`. Pin: body with `PREPARE`/`EXECUTE` → `hasDynamicSql true`; a table named ONLY inside the masked dynamic string → ZERO edge; a static `INSERT t` → `writes_to t`; a name absent from the body → NO edge; NEVER a self-edge. Spec: PREPARE/EXECUTE routine is flagged with no invented edges; Non-dynamic routine is NOT flagged; No edge defaulted for absent objects. Done: `npm test mysql/tokenizer`.
- [ ] 4.4 RED→GREEN `test/adapters/engines/mysql/map.test.ts` + `src/adapters/engines/mysql/map.ts`: `*Row` shapes + `buildMysqlRawCatalog(input, scope)` → deterministic `RawCatalog` (`engine: 'mysql'`). REUSE pg `KIND_RANK` + `compareObjects` (objects by `(kindRank, schema, name)`); `schema = TABLE_SCHEMA`; `schemas = [DATABASE()]`; columns by `ORDINAL_POSITION`; AUTO_INCREMENT via `ExtendedRawColumn & { extra: { autoIncrement: true } }` from `EXTRA LIKE '%auto_increment%'` (NO sequence object); generated via `extra: { generated: true, generationKind: 'STORED'|'VIRTUAL' }` + `GENERATION_EXPRESSION`; constraints by name (composite PK/FK/unique grouped, FK columns by `ORDINAL_POSITION`, ref aligned by `POSITION_IN_UNIQUE_CONSTRAINT`); CHECK carries `CHECK_CLAUSE`; indexes grouped by `INDEX_NAME` ordered by `SEQ_IN_INDEX`, `unique = (NON_UNIQUE === 0)`, `EXPRESSION`→`extra`, `SUB_PART`→`extra`; triggers grouped by name, `timing = ACTION_TIMING`, `events = [EVENT_MANIPULATION]`, `fires_on` destination = `EVENT_OBJECT_TABLE` (the parent table, NOT the trigger); comments inline (`TABLE_COMMENT`/`COLUMN_COMMENT`/`ROUTINE_COMMENT`, empty = absence). Body level-gating: `full`=body, `metadata`=no body, `off`=object absent. Edges via `tokenizeMysqlBody` at `confidence:'parsed'`. Spec: every per-object extraction Requirement above; AUTO_INCREMENT is represented on the column, never as a sequence. Done: `npm test mysql/map`.
- [ ] 4.5 GREEN `src/adapters/engines/mysql/mysql-schema-adapter.ts`: `MysqlSchemaAdapter` mirroring `PgSchemaAdapter` — `dialect`/`capabilities` getters; `extract(scope)` runs the catalog queries (parallel where independent) → `buildMysqlRawCatalog`, honoring levels; `fingerprint()` runs EXACTLY ONE query `SQL_MYSQL_FINGERPRINT` and returns ``sha256(`${table_count}|${column_count}|${routine_count}`)``; idempotent `close()` (`_closed` flag); extract/fingerprint-after-close → `ConnectionError`. (Exact `mysql:8` row shapes + the fp behavior are pinned by the golden in Batch 7.) Spec: fingerprint via one cheap catalog query (US-009); read strategy/lifecycle. Done: `npx tsc --noEmit`; `npm test mysql`.
- [ ] 4.6 Confirm US-031 scanner covers the new MySQL SQL: run `test/adapters/engines/security-scan.test.ts` — `queries.ts` SELECT strings pass with NO write-verb false-positive (no apostrophes/quoted examples in `mysql/**` JSDoc; `PREPARE`/`EXECUTE` regex literal + the `String.fromCharCode(96)` backtick are scanner-safe). Spec: mysql SQL passes the engines write-verb scanner (US-031). Done: `npm test security-scan`.

## Batch 5: Dispatch wiring + pinned message (+ code-4 regression guard)

> Satisfies `schema-extraction` "Supported dialects, capabilitiesFor and UnsupportedDialectError recognize mysql".
> CRITICAL: the pinned-error change is MESSAGE-ONLY. `exit-code.ts` maps `UnsupportedDialectError → 4` via
> `instanceof` (NOT the string), so `exit-code.ts` is UNTOUCHED — instead add an `instanceof` code-4 regression
> assertion. The message + its pinned assertion + the code-4 guard all land in THIS batch (the contract stays in sync).

- [ ] 5.1 RED→GREEN `src/infra/open-connections.ts`: add `createMysqlSchemaAdapter` to the `AdapterAndStore` adapter union + the local `let adapter:` union; add an `else if (resolved.dialect === 'mysql')` branch wiring `createMysqlSchemaAdapter({ host, port: parseInt(port), database, user, password, ssl: ssl === 'true' })` (conditional-spread optional fields). Spec: composition-root wiring; dispatch keys on the explicit dialect. Done: `npm test open-connections`.
- [ ] 5.2 RED→GREEN `src/index.ts`: add `case 'mysql'` to `capabilitiesFor()` returning `MYSQL_CAPABILITIES`; export `createMysqlSchemaAdapter` + `MYSQL_CAPABILITIES` from the barrel (composition root, ADR-004). Spec: mysql is a supported dialect and resolves its capabilities. Done: `npm test` (capabilitiesFor suite asserts `capabilitiesFor('mysql') === MYSQL_CAPABILITIES`).
- [ ] 5.3 RED→GREEN pinned message: change ONLY the `UnsupportedDialectError` message string in `src/core/errors.ts` to `Available dialects: sqlite, mssql, pg, mysql.` (class, `code: 'E_UNSUPPORTED_DIALECT'`, `instanceof` UNCHANGED). Add `test/core/errors-mysql.test.ts` (mirror `errors-pg.test.ts`) asserting the message `toContain('sqlite, mssql, pg, mysql')` + contains the bad-dialect name + unchanged `code`/`name`. Make NO change to `src/cli/exit-code.ts`. Spec: Pinned message and its assertion change together. Done: `npm test errors`.
- [ ] 5.4 RED→GREEN code-4 regression guard: add `test/cli/exit-code-mysql.test.ts` (mirror `exit-code-pg.test.ts`) asserting `exitCodeFor(new UnsupportedDialectError('mysql')) === 4` AND `exitCodeFor(new UnsupportedDialectError('redis')) === 4` via the `instanceof` path — proving the message change did NOT perturb the mapping, WITHOUT editing `exit-code.ts`. Spec: UnsupportedDialectError lists mysql and maps to exit code 4 (verified unchanged). Done: `npm test exit-code`.

## Batch 6: Permission doc + PermissionError doc-link + story reconciliation

> Satisfies `mysql-extraction` "Minimal read-only user documented" (US-033) and refines US-029 to MySQL-8-only.

- [ ] 6.1 GREEN `docs/permissions/mysql.md`: a MINIMAL read-only user/grant script — `CREATE USER`, grant ONLY the catalog/`information_schema` read access sufficient to read object definitions + view bodies (`VIEW_DEFINITION`) + routine bodies (`ROUTINE_DEFINITION`); NO broad table-data read grant. State it suffices to extract the full torture schema (catalog metadata only). Spec: Permission doc ships the minimal read-only user script. Done: doc present and reviewable.
- [ ] 6.2 Verify the `PermissionError` from 3.2 names the privilege AND links to `docs/permissions/mysql.md`; adjust the message if the path/text drifted. Spec: the message names the missing privilege and links to docs/permissions/mysql.md. Done: `npm test error-mapper` (assertion includes `docs/permissions/mysql.md`).
- [ ] 6.3 Refine `docs/stories/05-adapters.md` US-029 to MySQL-`8`-ONLY with the locked decisions (`schema == database`; AUTO_INCREMENT on the column, NO sequence; CHECK via `CHECK_CONSTRAINTS` 8.0.16+; `VIEW_DEFINITION` reparsed/truncation caveat, NO `SHOW CREATE VIEW`; `ROUTINE_DEFINITION`; presence-gated body tokenizer `confidence:'parsed'`, `supportsDependencyHints:false`; `PREPARE`/`EXECUTE` → `hasDynamicSql`; EVENTS out of scope; MariaDB sequences → `phase-8c`). In `docs/stories/06-security.md` (US-033) add the `mysql: docs/permissions/mysql.md` checkmark. Spec: US-029 refined to MySQL-8-only with the locked decisions encoded. Done: stories updated; no MariaDB/events claim remains in US-029.

## Batch 7: Torture fixture + Testcontainers E2E + CI + scanner sweep + lint (Docker-gated)

> Satisfies `mysql-extraction` "Committed MySQL torture fixture materialized via Testcontainers", "Golden-pinned
> RawCatalog and end-to-end pipeline", and the read-only/CI obligations. US-029. This is the Docker-dependent
> critical path; the gated job MUST NOT block the unit matrix. EXACT edge counts + endpoints + `stubCount:0` +
> no-self (the 8a CRITICAL-1 lesson) — existence-only assertions are FORBIDDEN.

- [ ] 7.1 GREEN `test/fixtures/mysql/torture.sql` (reviewable plain-text, NO binary blob) exercising 100% of `MYSQL_CAPABILITIES`: tables `products`/`orders`/`order_items`/`audit_log`; `audit_log.audit_id INT AUTO_INCREMENT PRIMARY KEY` (NO sequence); `order_items.total_price ... GENERATED ALWAYS AS (qty * unit_price) STORED`; a composite/ordered FK on `order_items (order_id, product_id)` + a CHECK `products (unit_price >= 0)` (8.0.16+) + PK + UNIQUE; a functional index `idx_products_name_lower ON products ((lower(name)))` (the `EXPRESSION` path) + a composite index (for `SEQ_IN_INDEX`); a view `v_order_summary` reading `orders` + `order_items`; a procedure writing TWO tables + reading ONE; a function writing ONE table + zero reads; a routine with a STATIC write AND a dynamic `PREPARE`/`EXECUTE` whose ONLY reference to a third table is INSIDE the prepared string (proves the mask gate); an `AFTER UPDATE ON orders` trigger; a `TABLE_COMMENT` + a `COLUMN_COMMENT`. Spec: Fixture is a reviewable .sql script; Fixture exercises the required torture objects. Done: file parses (applied in 7.2).
- [ ] 7.2 GREEN `test/fixtures/mysql/container.ts` (mirror `pg/container.ts`): image `mysql:8`, env `MYSQL_ROOT_PASSWORD`/`MYSQL_DATABASE`; wait-strategy = poll `SELECT 1` over a REAL `mysql2` connection (port-open is INSUFFICIENT — MySQL accepts seconds after the port opens), capped (per-suite 120s hookTimeout); apply `torture.sql` (`multipleStatements: true` on the SEED connection only, or split on `;`); expose `MysqlContainerHandle { config: MysqlAdapterConfig, stop() }`; `mysqlIntegrationEnabled()` + `describe.skipIf(!mysqlIntegrationEnabled())` with an explicit skip reason. Spec: Integration setup uses an ephemeral container and skips without DBGRAPH_INTEGRATION. Done: harness imports cleanly.
- [ ] 7.3 RED→GREEN `test/adapters/engines/mysql/extract.integration.test.ts` (skipIf-gated): connect via `createMysqlSchemaAdapter` → `extract` → serialize via `stableStringify` → seed `test/fixtures/mysql/golden-raw-catalog.json`; a SECOND extraction is BYTE-IDENTICAL. This run FINALIZES the exact `mysql:8` row shapes (the precise `EXTRA` string for a stored-generated column; `EXPRESSION`/`SUB_PART` presence). EXACT-set edge assertions (mirror `pg/extract.integration.test.ts`): the writer routine `deps.length === 3`, `writes === 2`, `reads === 1`, `expect(names).toEqual([...sorted])`, explicit no-self + no-phantom-to-absent; the view `expect(names).toEqual(['order_items','orders'])` + no-self + no-phantom; the dynamic routine `hasDynamicSql === true` AND `deps.length === 0`; the trigger `timing:'AFTER'`/`events:['UPDATE']`/parent `orders`; golden has ZERO `sequence` objects; the AUTO_INCREMENT column carries `extra.autoIncrement: true`. Spec: RawCatalog golden is deterministic and byte-identical; Routine/View parsed edge sets; PREPARE/EXECUTE flagged with no invented edges; Trigger carries event + timing. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.4 RED→GREEN fingerprint DDL/DML integration test (in the extract suite, gated): `CREATE TABLE` → `fingerprint()` CHANGES; `ALTER TABLE ... ADD COLUMN` → CHANGES (the `column_count` marker moves); `INSERT` → UNCHANGED; assert the value is a 64-char hex string AND `fingerprint()` issues exactly ONE query (does NOT enumerate objects). Spec: fingerprint changes on ADD COLUMN; fingerprint changes on other DDL; fingerprint is stable across data-only changes; one cheap query. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.5 RED→GREEN database-scope + level integration assertions (gated): a second database `other` on the same server → the catalog contains ONLY `app` objects, none from `other`; `RawCatalog.schemas` is exactly `['app']`; a view at `metadata` present without body, a procedure type `off` absent. Spec: Only the connected database is extracted; RawCatalog.schemas is the single connected database; Metadata level omits the body, off level omits the object. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.6 RED→GREEN `test/adapters/engines/mysql/e2e.integration.test.ts` (gated): `extract` → `normalizeCatalog` → `SqliteGraphStore.upsertGraph` → `impact`/`path` queries → seed `test/fixtures/mysql/golden-e2e.json`; byte-identical re-run. Pin exact edge counts AND BOTH endpoints of every edge; assert `stubCount: 0` and NO self-reference edge exists. Spec: Full pipeline reaches impact and path queries golden-pinned with exact edges. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration`.
- [ ] 7.7 GREEN `package.json`: add `mysql2` to `optionalDependencies` (ADR-002/006 pre-approved; justify in commit body); `testcontainers` already present; `npm ci` succeeds. Spec: NO new runtime deps beyond the optional mysql2. Done: `mysql2` under `optionalDependencies`; `npm ci` clean.
- [ ] 7.8 GREEN `.github/workflows/ci.yml`: KEEP the existing `test` + `mssql-integration` + `pg-integration` jobs unchanged; ADD a separate gated `mysql-integration` job (Linux-only, `needs: []`) setting `DBGRAPH_INTEGRATION=1`, running ONLY `npm run test:integration` (or the mysql subset), using ONLY an ephemeral `mysql:8` container, never referencing any validation database, never required by the unit matrix. Spec: mysql CI job is gated and never touches a validation database. Done: YAML valid; unit matrix does NOT depend on `mysql-integration`.
- [ ] 7.9 Final GATE + closeout: `npx tsc --noEmit` clean; `npm run lint` 0/0; `npm test` green incl. `security-scan.test.ts` over `engines/**` (catalog SELECTs only — no `SET TRANSACTION READ ONLY`, no write verbs, no `SHOW CREATE VIEW`); `DBGRAPH_INTEGRATION=1 npm run test:integration` green against REAL `mysql:8`. Confirm 100% of `MYSQL_CAPABILITIES` is exercised by the torture fixture. Record learnings in `docs/learnings.md`. Spec: write-verb scanner stays green; CI green including the gated mysql-integration job. Done: all gates green.

## Apply Batch Grouping (one sub-agent session each)

- **Batch 1** (1.1–1.5): PROMOTE `maskDynamicStrings` + `bodyContainsRef` into `_shared/tokenizer-core.ts`, refactor `pg/tokenizer.ts`, confirm `REPLACE` in `WRITE_VERB_PATTERNS`, prove pg goldens + pg tokenizer byte-identical — NO MySQL code, NO DB beyond the gated pg golden re-run.
- **Batch 2** (2.1–2.5): `MYSQL_CAPABILITIES`, `MysqlAdapterConfig` union widening + the `host` JSDoc fix, `MysqlSource` config + parse-config `mysql` branch — pure logic, NO connection.
- **Batch 3** (3.1–3.3): driver seam, error-mapper, factory (lazy `import('mysql2/promise')`) — pure/synthetic, NO DB.
- **Batch 4** (4.1–4.6): queries, captured-row fixtures, MySQL tokenizer (`PREPARE`/`EXECUTE` + presence gate), map, adapter class + fingerprint, US-031 scanner confirm — pure logic, NO DB.
- **Batch 5** (5.1–5.4): dispatch wiring (`open-connections`, barrel/`capabilitiesFor`), pinned message + code-4 regression guard.
- **Batch 6** (6.1–6.3): `docs/permissions/mysql.md`, `PermissionError` doc-link verify, US-029 + US-033 story reconciliation.
- **Batch 7** (7.1–7.9): torture fixture, Testcontainers harness, RawCatalog golden + fingerprint DDL/DML + scope/level + E2E, `mysql2` optionalDependency, gated `mysql-integration` CI, final scanner/lint/closeout — Docker-gated critical path.

### Dependency bottlenecks

- **Batch 1 gates EVERYTHING.** The `_shared/` PROMOTION must land AND the pg goldens + pg tokenizer suite must stay byte-identical (1.5) before the MySQL tokenizer (4.3) consumes the shared `maskDynamicStrings`/`bodyContainsRef`. A pg golden drift here is a hard STOP. 1.4 (the `REPLACE` confirmation) must precede any MySQL write-verb classification.
- **2.2 (union widening + JSDoc fix) gates all MySQL modules** referencing `MysqlAdapterConfig` (3.3 factory, 4.5 adapter, 5.1 wiring); the prior-phase green check (2.3) must pass before wiring.
- **3.1 (driver seam) gates 3.3** (factory wraps it) and **4.5** (adapter talks only to it); **3.2 (error-mapper) gates 3.3** (factory maps connect errors) and **6.2** (doc-link).
- **4.1 queries + 4.4 map + 4.3 tokenizer** must precede **4.5** (adapter wires them) and **7.3/7.6** (goldens assert their output).
- **5.3 message + 5.4 code-4 guard are atomic** — they MUST land in the same batch as one contract; splitting them risks a red pinned assertion or an unverified exit-code mapping.
- **7.1 torture.sql + 7.2 container gate ALL integration (7.3–7.6)** — the Docker-dependent critical path. The exact `mysql:8` row shapes (the `EXTRA` string for a stored-generated column; `EXPRESSION`/`SUB_PART` presence) and the `fingerprint` behavior are FINALIZED by 7.3/7.4 against the live container, so 4.5's adapter is provisional until pinned.
- **7.7 (`mysql2` optionalDependency) + the `test:integration` script** must be in place before 7.8's CI job and the 7.3–7.6 done-checks can run cleanly.

## Definition of Done (tied to the proposal's Success Criteria)

- [ ] `MysqlSchemaAdapter` implements the EXISTING `SchemaAdapter` port (no port SHAPE change), wired via `createMysqlSchemaAdapter` through the composition root (`open-connections.ts` + `src/index.ts`). — Batches 2, 3, 5
- [ ] Extracts a MySQL `8` torture schema into a coherent `RawCatalog`: the connected database AS the schema; tables/columns/types/nullability/defaults; AUTO_INCREMENT on the column (NO sequence node); PK/FK (incl. composite)/unique/CHECK; indexes; views (`VIEW_DEFINITION`); procedures/functions (`ROUTINE_DEFINITION`); triggers (event+timing); comments. — Batches 4, 7
- [ ] `reads_from`/`writes_to` classified from view/routine bodies at `confidence:'parsed'` via the SHARED tokenizer; `PREPARE`/`EXECUTE` marks `hasDynamicSql:true`; `supportsDependencyHints:false`. EXACT-set negative assertions prove NO phantom and NO self edges (8a CRITICAL-1 designed out). — Batches 4, 7
- [ ] `MYSQL_CAPABILITIES` truthful: table/column/constraint/index/view/procedure/function/trigger; NO `sequence`; `supportsBodies:true`; `supportsDependencyHints:false`; 100% matrix coverage by the fixture. — Batches 2, 7
- [ ] The `_shared/` PROMOTION lands: `maskDynamicStrings` + `bodyContainsRef` in `_shared/tokenizer-core.ts`, consumed by both pg and mysql; pg goldens + pg tokenizer suite BYTE-IDENTICAL after the refactor. — Batch 1
- [ ] `schema == database`: every object carries the connected database as its schema; `RawCatalog.schemas` is that one database; every catalog query is filtered `TABLE_SCHEMA = DATABASE()`. — Batches 4, 7
- [ ] Connectivity via host/port (3306)/database/user/password (+ optional ssl); `password` is `${env:VAR}` env-only; NO `schema?` knob; absent `mysql2` raises `ConnectionError('npm i mysql2')`. — Batches 2, 3
- [ ] `docs/permissions/mysql.md` ships the minimal read-only user; a missing privilege raises an actionable `PermissionError` naming the privilege + doc; no `SET TRANSACTION READ ONLY` workaround; NO `SHOW CREATE VIEW`. — Batches 3, 6
- [ ] `fingerprint()` returns a cheap one-query value that MOVES on DDL including `ADD COLUMN` and is STABLE on data-only DML. — Batches 4, 7
- [ ] Determinism (ADR-008): the `RawCatalog` is golden-pinned and byte-identical; the E2E pins exact edge counts AND endpoints, `stubCount:0`, no self-edge. — Batch 7
- [ ] The write-verb scanner over `src/adapters/engines/**` stays green (catalog SELECTs only; no scanner-false JSDoc literals; `REPLACE` covered). — Batches 1, 4, 7
- [ ] NO new runtime deps beyond optional `mysql2`; `tsc`/lint/test clean; CI green including the gated `mysql-integration` job (`DBGRAPH_INTEGRATION=1`), which never blocks the unit matrix. — Batch 7
- [ ] The pinned `UnsupportedDialectError` message updated to `sqlite, mssql, pg, mysql` with its assertion AND the exit-code-4 mapping verified unchanged (via `instanceof` guard, `exit-code.ts` untouched). — Batch 5
- [ ] US-029 refined to MySQL-`8`-only with the locked decisions encoded; MariaDB sequences recorded as the pre-planned `phase-8c` follow-up. — Batch 6
