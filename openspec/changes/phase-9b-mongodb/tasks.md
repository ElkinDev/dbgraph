# Tasks: Phase 9b — MongoDB Schema-Extraction Adapter

Standing header (every task): STRICT TDD (RED→GREEN→refactor; the failing test PRECEDES the code; NEVER mock the
`mongodb` driver). Pure units (type-map, map, error-mapper, capabilities, parse-config, resolve-secrets, config-union)
use fake cursors / synthetic error objects / inline `RawObject` literals — NO DB. The live sampling contract is
integration-first against a REAL `mongo:7` Testcontainers instance, gated by `DBGRAPH_INTEGRATION=1`. Mirror the
archived `pg/` + `mysql/` adapter SHAPE file-for-file (thin class + a SINGLE duck-typed `MongodbReadonlyDriver` seam +
a `MongodbCapabilityProbe` mirroring `PgCapabilityProbe`) — do NOT scaffold a `mongodb/strategies/` tree (that is
SQL-Server-only machinery). `createMongodbSchemaAdapter` is the ONLY join point (ADR-006, lazy
`import('mongodb' as string)` confined to `factory.ts` + `probe.ts`). NO new runtime dep beyond the optional `mongodb`
+ the existing `testcontainers` dev dep. Honor: hexagonal ADR-004 (core imports nothing outward; downstream
`normalizeCatalog → SqliteGraphStore → query` pipeline UNCHANGED), determinism ADR-008, strict TS / no `any`,
`exactOptionalPropertyTypes` (conditional spread). `path.win32`/`path.posix` for ANY path logic — no hard-coded
separators (URI-based adapter has none, but fixtures do). Reuse `ConnectionError`/`PermissionError`/
`ConnectivityUnavailableError` — NO new error types. **Sampled VALUES are NEVER persisted: discard every document
after the type-merge — only the path→{types,count} accumulator survives.** EXACT-set assertions from the start
(`toEqual` sorted endpoints, `toBe(N)` counts, explicit no-self + no-phantom) — existence-only `.toBeDefined()` is
FORBIDDEN (L-009). Conventional commits with US-030; English.

Per-batch GATE (ALL must pass before the next batch): `npx tsc --noEmit` clean · `npm run lint` 0 errors / 0 warnings ·
`npm test` green INCLUDING the write-verb scanner `test/adapters/engines/security-scan.test.ts` over `engines/**` ·
plus the batch-specific proof noted in each section. Batch 1 additionally re-proves the SQL normalize goldens
BYTE-IDENTICAL (`git diff --exit-code test/golden/normalize/` empty). Batch 7 additionally runs
`DBGRAPH_INTEGRATION=1 npm run test:integration` green against a REAL `mongo:7` container.

## Batch 1: `RawField`/`FieldPayload` + normalizer `fields` branch — prove SQL goldens BYTE-IDENTICAL (NO Mongo code)

> Satisfies `schema-extraction` "Optional RawField model path for schemaless field structure". This is the
> load-bearing no-CI gate: SQL engines (sqlite/mssql/pg/mysql) NEVER set `RawObject.fields`, so the additive branch
> MUST be provably inert — the SQL normalize goldens MUST stay byte-identical. A golden drift here is a HARD STOP.
> `dataType` is a STRING (union form `int|string`, sorted) — NOT a `types[]` array — so `inferReferences` (reads
> `payload.dataType` as a string) consumes a `field` node identically to a `column` node. NO Mongo code in this batch.

- [x] 1.1 RED→GREEN `test/core/model/catalog.test.ts` (extend) + `src/core/model/catalog.ts`: add `RawField { readonly name: string; readonly dataType: string; readonly frequency: number; readonly nullable?: boolean }` + `readonly fields?: readonly RawField[]` on `RawObject`. Assert a `RawObject` of `kind:'collection'` accepts `fields` and that `dataType` is typed `string`. Done: `npx tsc --noEmit`.
- [x] 1.2 RED→GREEN `test/core/model/node.test.ts` (extend) + `src/core/model/node.ts`: add a `FieldPayload` accessor view exposing `dataType`, `frequency`, `nullable?`. The `'field'` `NodeKind`, `levels.fields` and `getLevelForKind('field')` ALREADY exist — do NOT re-add them; only the payload accessor is missing. Spec: a `'field'` node is consumable by `inferReferences` exactly as a `'column'` node (reads `payload.dataType`). Done: `npm test node`.
- [x] 1.3 RED→GREEN `test/core/normalize/normalize.test.ts` (extend) + `src/core/normalize/normalize.ts`: add `buildFieldNode` + a `fields` branch in `buildChildNodes` (MIRROR the `columns` branch; gate on `scope.levels.fields`). Pin: a `RawObject` of `kind:'collection'` with `fields` → one `'field'` node per `RawField` carrying `dataType`+`frequency`; absent `fields` → ZERO `'field'` nodes; level `off` for fields → branch skipped. Spec: A RawObject with fields normalizes into field nodes. Done: `npm test normalize`.
- [x] 1.4 GATE — prove byte-identical: run the SQL normalize golden suite, then `git diff --exit-code test/golden/normalize/` MUST be EMPTY (sqlite/mssql/pg/mysql set no `fields` → no `'field'` node → identical output). ANY drift is a HARD STOP — the branch leaked; investigate, do NOT re-bless. Spec: SQL engines leaving fields unset stay byte-identical (ADR-008). Done: golden diff empty; `tsc`/lint/`npm test` green.

## Batch 2: Capabilities + config plumbing (no live connection)

> Satisfies `mongodb-extraction` "Truthful MongoDB CapabilityMatrix" + "Connectivity via a URI reference ..." (parse
> half), and `schema-extraction` "SchemaAdapter port lives in core" + "Supported dialects ... recognize mongodb"
> (SUPPORTED_DIALECTS half). `MongodbAdapterConfig` is URI-based: NO host/port/user/password/schema decomposition.

- [ ] 2.1 RED→GREEN `test/adapters/engines/mongodb/capabilities.test.ts` + `src/adapters/engines/mongodb/capabilities.ts`: `MONGODB_CAPABILITIES: CapabilityMatrix`, `engine:'mongodb'`; `supported = new Set` of `collection, field, index` (verbatim `NodeKind` names); `supportsBodies:false`; `supportsDependencyHints:false`; `defaultLevels = DEFAULT_LEVELS`. Assert the set EXCLUDES `table, column, constraint, view, procedure, function, trigger, sequence`. Spec: Matrix declares collection/field/index supported and the SQL types unsupported; Matrix reports supportsBodies false and supportsDependencyHints false. Done: `npm test mongodb/capabilities`.
- [ ] 2.2 RED→GREEN `src/core/ports/schema-adapter.ts`: add `MongodbAdapterConfig` (`uri: string` — `${env:VAR}` reference; `database: string`; `sampleSize?: number` default 100; `tls?: boolean`; NO `schema?`, NO host/port/user/password) + extend the `SchemaAdapterConfig` union. Note in JSDoc that dispatch keys on the explicit `dialect` field, NOT shape. Re-export through `src/core/ports/index.ts` + `src/core/index.ts`. Spec: SchemaAdapterConfig union includes the mongodb variant without a shape change; carries a URI/database/sampleSize?/tls? and NO schema/host/port fields. Done: `npx tsc --noEmit`; sqlite/mssql/pg/mysql members untouched.
- [ ] 2.3 Verify prior phases stay green after widening: existing SQLite/MSSQL/PG/MySQL factories + tests compile and pass unchanged. Spec: no new method is added to the SchemaAdapter port (the port SHAPE is unchanged). Done: `npm test` (all prior suites pass).
- [ ] 2.4 RED→GREEN `src/infra/config/schema.ts`: add `'mongodb'` to `SUPPORTED_DIALECTS`; add `MongodbSource` (`uri`/`database` strings, `sampleSize?`/`tls?`; NO host/port/schema) + the `mongodb` `DbgraphConfig` member. Spec: SUPPORTED_DIALECTS includes 'mongodb'. Done: `npx tsc --noEmit`.
- [ ] 2.5 RED→GREEN `test/infra/config/parse-config.test.ts` (extend) + `src/infra/config/parse-config.ts`: add `parseMongodbSource` + `case 'mongodb'`. REQUIRE `uri`+`database`; REJECT a LITERAL `uri` (reuse `isEnvRef`/`ENV_REF_RE` — require `${env:VAR}`); default `sampleSize` to 100; optional `tls`; NO `schema`/host/port. Unknown dialect STILL throws `UnsupportedDialectError`. Spec: URI must be supplied by env reference, not a literal; Connects with a URI reference and default sample size. Done: `npm test parse-config`.
- [ ] 2.6 RED→GREEN `test/infra/config/resolve-secrets.test.ts` (extend) + `src/infra/config/resolve-secrets.ts`: add `resolveMongodbSource` + `case 'mongodb'` resolving the `${env:VAR}` URI from the environment (mirror the pg/mysql password resolution — easy to forget). Spec: the URI `${env:VAR}` is resolved env-only. Done: `npm test resolve-secrets`.

## Batch 3: Driver seam + error-mapper + factory + probe (lazy import, synthetic errors — NO DB)

> Satisfies `mongodb-extraction` "Connectivity via a URI reference ..." (connect half), "Authentication failure raises
> an actionable ConnectionError", "Minimal read-only role ... actionable PermissionError" (mapper half), and "Missing
> mongodb driver names the install command". Lazy `import('mongodb' as string)` lives ONLY in `factory.ts` + `probe.ts`.

- [ ] 3.1 GREEN `src/adapters/engines/mongodb/driver.ts`: `MongodbReadonlyDriver { listCollections(): Promise<readonly {name:string;options?:Record<string,unknown>}[]>; sample(collection,size): Promise<readonly Record<string,unknown>[]>; listIndexes(collection): Promise<readonly Record<string,unknown>[]>; command(cmd): Promise<Record<string,unknown>>; close(): Promise<void> }` + a `MongoClientLike` duck-type and `createMongodbReadonlyDriver(client, database)` adapting `db.listCollections().toArray()`, `collection.aggregate([{$sample:{size}}]).toArray()`, `collection.listIndexes().toArray()`, `db.command(cmd)`. NO top-level `mongodb` import (ADR-004/006). Done: `npx tsc --noEmit`.
- [ ] 3.2 RED→GREEN `test/adapters/engines/mongodb/error-mapper.test.ts` + `src/adapters/engines/mongodb/error-mapper.ts`: PURE `mapMongoError(cause)` over synthetic `{ code, codeName, name, message }` — NO DB. code `18`/`AuthenticationFailed` → `ConnectionError` (check URI credentials); code `13`/`Unauthorized`/role → `PermissionError` naming the privilege + `docs/permissions/mongodb.md`; `MongoServerSelectionError`/`ECONNREFUSED`/timeout → `ConnectionError` (verify URI host + reachability); `MODULE_NOT_FOUND` → `ConnectivityUnavailableError` (`npm i mongodb`). Content-free summaries (NO host/URI in message); raw cause on `error.cause` only. Spec: Authentication failure raises an actionable ConnectionError; Missing privilege yields a typed actionable PermissionError. Done: `npm test mongodb/error-mapper`.
- [ ] 3.3 RED→GREEN `test/adapters/engines/mongodb/factory.test.ts` + `src/adapters/engines/mongodb/factory.ts`: `createMongodbSchemaAdapter(config, deps?)` — lazy `import('mongodb' as string)`, `new MongoClient(uri, { tls? }).connect()`, map connect failures via `mapMongoError`, wrap in `createMongodbReadonlyDriver(client, database)` → `MongodbSchemaAdapter`. A `MODULE_NOT_FOUND` from the dynamic import MUST raise an error whose message contains the exact `npm i mongodb`. `createMongodbSchemaAdapter` is the ONLY join point. Spec: Absent mongodb driver names npm i mongodb. Done: `npm test mongodb/factory` (missing-driver suite asserts `npm i mongodb`).
- [ ] 3.4 RED→GREEN `test/adapters/engines/mongodb/probe.test.ts` + `src/adapters/engines/mongodb/probe.ts`: `MongodbCapabilityProbe` mirroring `PgCapabilityProbe` — `engine:'mongodb'`; `probe()` resolves NEVER rejects + NEVER opens a connection; `nativeDriver` via injectable `import('mongodb' as string)` seam (resolves→true, MODULE_NOT_FOUND→false); `cliTools` via injectable `spawnSync` where/which `mongosh` (cross-platform); `odbc:false`. Spec: probe is connection-free and never rejects (resilient-connectivity contract). Done: `npm test mongodb/probe`.
- [ ] 3.5 GREEN `src/adapters/engines/mongodb/mongodb-schema-adapter.ts` (skeleton): `MongodbSchemaAdapter` mirroring `PgSchemaAdapter` — `dialect:'mongodb'`/`capabilities` getters; `extract`/`fingerprint` stubs throwing `not implemented` (filled in Batch 4); idempotent `close()` (`_closed` flag) delegating to the driver; extract/fingerprint-after-close → `ConnectionError`. Done: `npx tsc --noEmit`.

## Batch 4: Sampling + type-merge + DISCARD + `type-map.ts` + `map.ts` (pure logic, fake cursors — NO DB)

> Satisfies `mongodb-extraction` "Infer fields by sampling, merging observed types into a union with presence
> frequency", "Represent nested documents as dotted paths and arrays by element type", "Sampled values are NEVER
> persisted", "Extract indexes via listIndexes", "Carry top-level $jsonSchema validators in extra", and "fingerprint
> via dbStats". US-030 / US-009. Read-only by construction (US-031): no write op may survive the scanner.

- [ ] 4.1 RED→GREEN `test/adapters/engines/mongodb/type-map.test.ts` + `src/adapters/engines/mongodb/type-map.ts`: PINNED `bsonToDataType(value)` — objectId→`'objectId'`; int|long→`'int'`; double|decimal128→`'numeric'`; string→`'string'`; bool→`'bool'`; date→`'date'`; null→`'null'`; array→`'<elemType>[]'` (recurse element); object→handled by the walk (dotted recursion); unknown BSON→`'unknown'` (NO throw). Plus `mergeDataTypes(types: Set<string>): string` → SORTED union join (`int|string`). Pin every row + the mixed-union sort. Spec: A field with mixed BSON types carries the union of those types; the pinned BSON→dataType table. Done: `npm test mongodb/type-map`.
- [ ] 4.2 RED→GREEN `test/adapters/engines/mongodb/sample-walk.test.ts` + `src/adapters/engines/mongodb/sample-walk.ts` (or co-located in `map.ts`): `sampleCollections(docs)` walks each doc's keys into a `Map<path,{types:Set<string>;count:number}>` — nested objects → dotted `address.city`; arrays of sub-docs → `items[].sku`; missing key → NOT counted; then computes `frequency = count / sampled` and `dataType = mergeDataTypes(types)`. **DISCARD invariant**: assert the returned accumulator/`RawField[]` contains NO document VALUE (only path/dataType/frequency) — feed a doc with a unique sentinel value and assert it appears NOWHERE in the output. Spec: Fixed dataset yields exact field types and frequencies; Nested document keys become dotted field paths; Array elements are encoded by element type; RawCatalog carries field metadata but no document values. Done: `npm test mongodb/sample-walk`.
- [ ] 4.3 RED→GREEN `test/adapters/engines/mongodb/map.test.ts` + `src/adapters/engines/mongodb/map.ts`: `buildMongodbRawCatalog(sampledCollections, indexes, validators)` → deterministic `RawCatalog` (`engine:'mongodb'`) using fake cursor inputs. Each collection → `RawObject` kind `collection`, `schema = database` (NEVER null), `fields` SORTED by path (each a `RawField` from 4.2); `RawIndex` from `listIndexes` (unique flag; COMPOUND = ordered keys preserved as ONE index; `_id` index attributed once, not double-counted); top-level `$jsonSchema` `required`+`properties` in `extra` (deep nesting NOT walked); EXCLUDE `system.*`/`admin`/`local`/`config`; a collection `off` by scope → absent. REUSE the pg/mysql `KIND_RANK`/`compareObjects` ordering. Spec: Extract collections as kind collection; A collection type set to off is absent; Unique and compound indexes; Top-level $jsonSchema required and properties in extra; A collection without a validator carries no validator metadata. Done: `npm test mongodb/map`.
- [ ] 4.4 GREEN fill `src/adapters/engines/mongodb/mongodb-schema-adapter.ts`: `extract(scope)` → `driver.listCollections()` (filter system + scope), per collection `driver.sample(name, size)` with `size = min(config.sampleSize ?? 100, /* clamp */)` → `sampleCollections` → DISCARD docs → `driver.listIndexes(name)` + `driver.command({listCollections...})` validator → `buildMongodbRawCatalog`; honor `scope.levels`. `fingerprint()` → `driver.command({dbStats:1})` → ``sha256(`${collections}|${indexes}|${objects}`)`` (64-char hex; MUST NOT walk documents). (Exact `mongo:7` shapes + the fp behavior are pinned by the golden in Batch 7.) Spec: fingerprint via dbStats; Sampled values are NEVER persisted. Done: `npx tsc --noEmit`; `npm test mongodb`.
- [ ] 4.5 Confirm US-031 scanner covers the new Mongo files: run `test/adapters/engines/security-scan.test.ts` — `driver.ts`/`map.ts`/`sample-walk.ts` carry NO executable write verb and NO write-verb false-positive in JSDoc (`$sample`/`find`/`listCollections`/`listIndexes`/`command` only). Spec: mongodb adapter passes the engines write-verb scanner; Adapter issues only read operations. Done: `npm test security-scan`.

## Batch 5: Dispatch wiring + pinned message (+ code-4 regression guard) + inference-on

> Satisfies `schema-extraction` "Supported dialects, capabilitiesFor and UnsupportedDialectError recognize mongodb".
> CRITICAL: the pinned-error change is MESSAGE-ONLY. `exit-code.ts` maps `UnsupportedDialectError → 4` via `instanceof`
> (NOT the string), so `exit-code.ts` is UNTOUCHED — add an `instanceof` code-4 regression assertion instead. Inference
> needs NO infer-engine change: the `collection`/`field` node presence auto-fires `inferReferences`.

- [ ] 5.1 RED→GREEN `src/infra/open-connections.ts`: add `createMongodbSchemaAdapter` to the `AdapterAndStore` adapter union + the local `let adapter:` union; add an `else if (resolved.dialect === 'mongodb')` branch wiring `createMongodbSchemaAdapter({ uri, database, sampleSize?, tls? })` (conditional-spread optional fields). Spec: composition-root wiring; dispatch keys on the explicit dialect. Done: `npm test open-connections`.
- [ ] 5.2 RED→GREEN `src/index.ts`: add `case 'mongodb'` to `capabilitiesFor()` returning `MONGODB_CAPABILITIES`; export `createMongodbSchemaAdapter` + `MongodbCapabilityProbe` + `MONGODB_CAPABILITIES` from the barrel (composition root, ADR-004). Spec: mongodb is a supported dialect and resolves its capabilities. Done: `npm test` (capabilitiesFor suite asserts `capabilitiesFor('mongodb') === MONGODB_CAPABILITIES`).
- [ ] 5.3 RED→GREEN pinned message: change ONLY the `UnsupportedDialectError` message string in `src/core/errors.ts` to list `sqlite, mssql, pg, mysql, mongodb` (class, `code`, `instanceof` UNCHANGED). Add `test/core/errors-mongodb.test.ts` (mirror `errors-mysql.test.ts`) asserting the message `toContain('sqlite, mssql, pg, mysql, mongodb')` + contains the bad-dialect name + unchanged `code`/`name`. Make NO change to `src/cli/exit-code.ts`. Spec: Pinned message and its assertion change together. Done: `npm test errors`.
- [ ] 5.4 RED→GREEN code-4 regression guard: add `test/cli/exit-code-mongodb.test.ts` (mirror `exit-code-mysql.test.ts`) asserting `exitCodeFor(new UnsupportedDialectError('mongodb')) === 4` AND `exitCodeFor(new UnsupportedDialectError('redis')) === 4` via the `instanceof` path — proving the message change did NOT perturb the mapping, WITHOUT editing `exit-code.ts`. Spec: UnsupportedDialectError lists mongodb and maps to exit code 4 (verified unchanged). Done: `npm test exit-code`.
- [ ] 5.5 RED→GREEN `test/core/normalize/infer-on-fields.test.ts` (unit, NO DB): a `RawCatalog` with `collection` nodes `orders` (field `customer_id`, `dataType:'objectId'`) + `customers` (field `_id`, `dataType:'objectId'`) → `normalizeCatalog` auto-fires `inferReferences` (`hasCollectionOrFieldNode`) → ONE `inferred_reference` edge `orders.customer_id` → `customers._id`, `confidence:'inferred'`, numeric `score`; `_id` yields no source entity (bare `id`→null, target-only); NO declared `references` edge. EXACT endpoints + `toBe(1)` count + no-self + no-phantom. Spec: orders.customer_id infers a reference to customers._id; Inference fires automatically from the presence of a collection/field node. Done: `npm test infer-on-fields`.

## Batch 6: Permission doc + PermissionError doc-link + story reconciliation

> Satisfies `mongodb-extraction` "Minimal read-only role documented with actionable PermissionError" (US-033) and
> refines US-030; reconciles US-031/033.

- [ ] 6.1 GREEN `docs/permissions/mongodb.md`: a MINIMAL read-only role — `read` on the target database ONLY; enumerate the privileges sampling-based extraction needs (`dbStats`, `listCollections`, `listIndexes`, `find`/`$sample`); NO write or admin grants. State it suffices to extract the full torture dataset (metadata + sampling only). Use explicit `path.win32`/`path.posix` if the doc references any path. Spec: Permission doc ships the minimal read-only role. Done: doc present and reviewable.
- [ ] 6.2 Verify the `PermissionError` from 3.2 names the privilege AND links to `docs/permissions/mongodb.md`; adjust the message if the path/text drifted. Spec: the message names the missing privilege and links to docs/permissions/mongodb.md. Done: `npm test mongodb/error-mapper` (assertion includes `docs/permissions/mongodb.md`).
- [ ] 6.3 Refine `docs/stories/05-adapters.md` US-030 to the locked decisions (`schema == database`; sampling is the SOLE structure source; typed `RawField` + `RawObject.fields` → `'field'` nodes; dotted paths + `items[]` element encoding; type-MERGE union + frequency; values discarded post-merge; `supportsBodies:false`; `supportsDependencyHints:false`; refs ONLY as `<entity>_id`→`<collection>._id` inferred edges; deep `$jsonSchema` out of scope, top-level in `extra`). In `docs/stories/06-security.md` add the `mongodb: docs/permissions/mongodb.md` checkmark (US-033) and the values-never-persisted note (US-031). Spec: US-030 refined with the locked decisions encoded. Done: stories updated.

## Batch 7: Torture dataset + Testcontainers E2E + inference integration + CI + scanner sweep (Docker-gated)

> Satisfies `mongodb-extraction` "Committed torture dataset materialized via gated Testcontainers", "Golden-pinned
> RawCatalog and end-to-end pipeline with inferred edges", and the read-only/CI obligations. US-030. Docker-dependent
> critical path; the gated job MUST NOT block the unit matrix. EXACT edge endpoints + count (L-009) — existence-only
> is FORBIDDEN. `$sample(size ≥ doc_count)` so the sample is the FULL dataset → deterministic golden.

- [ ] 7.1 GREEN `test/fixtures/mongodb/torture.ts` (reviewable programmatic seed — plain TS inserts, NO binary dump; mirror `mysql/container.ts` seeding) exercising 100% of `MONGODB_CAPABILITIES`: `customers` (with `_id`); `orders` (with `customer_id`→`customers._id` of compatible objectId type + a `$jsonSchema` validator declaring top-level `required`+`properties`); `products`; `events` (a nested sub-doc `address.city`, an array of sub-docs `items[].sku`, an always-present field, a sometimes-present field at ~0.87 frequency, a mixed-BSON-type field `int|string`); a UNIQUE single-key index + a COMPOUND two-key index. Spec: Fixture is a reviewable seed exercising the required torture objects; 100% matrix coverage. Done: seed module imports cleanly (applied in 7.2).
- [ ] 7.2 GREEN `test/fixtures/mongodb/container.ts` (mirror `pg/container.ts`): image `mongo:7`; wait-strategy = poll a real `mongodb` ping/command (port-open INSUFFICIENT), capped (per-suite hookTimeout); apply the `torture.ts` seed via a SEED connection ONLY; expose `MongodbContainerHandle { config: MongodbAdapterConfig, stop() }`; `mongodbIntegrationEnabled()` (`DBGRAPH_INTEGRATION=1`, NOT mere Docker presence) + `describe.skipIf(!mongodbIntegrationEnabled())` with an explicit skip reason. Spec: Integration setup uses an ephemeral container and skips without DBGRAPH_INTEGRATION. Done: harness imports cleanly.
- [ ] 7.3 RED→GREEN `test/adapters/engines/mongodb/extract.integration.test.ts` (skipIf-gated): connect via `createMongodbSchemaAdapter` → `extract` with `$sample(size ≥ doc_count)` → serialize via `stableStringify` → seed `test/fixtures/mongodb/golden-raw-catalog.json`; a SECOND extraction is BYTE-IDENTICAL. This FINALIZES the exact `mongo:7` shapes. Assert: `email` dataType `string` frequency `1.0`; the sometimes-present field frequency `< 1.0` (the fixed `0.87`); the mixed field dataType union `int|string`; `address.city` + `items[].sku` paths present; the unique index marked unique; the compound index ONE index with ordered keys; top-level `$jsonSchema` in `extra`; ZERO `system.*` collections. Spec: RawCatalog golden is deterministic and byte-identical; Fixed dataset yields exact field types and frequencies; Unique and compound indexes; Top-level $jsonSchema in extra. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration` green; golden seeded + byte-identical on second run.
- [ ] 7.4 RED→GREEN values-never-persisted + fingerprint integration (in the extract suite, gated): (a) seed a DISTINCTIVE sentinel value (e.g. a unique email literal) → assert it appears NOWHERE in the serialized `RawCatalog`. (b) `fingerprint()` is a 64-char hex from `dbStats`; CREATE a collection or index → fingerprint CHANGES; INSERT documents only → UNCHANGED; assert it does NOT enumerate documents. Spec: No sampled document value appears; fingerprint changes on collection or index DDL; fingerprint is stable across data-only changes. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration` green.
- [ ] 7.5 RED→GREEN `test/adapters/engines/mongodb/inference.integration.test.ts` + `e2e.integration.test.ts` (gated): `extract` → `normalizeCatalog` (inference ON, auto-fired) → `SqliteGraphStore.upsertGraph` → `impact`/`path` query. Pin the `inferred_reference` edge `orders.customer_id` → `customers._id` by BOTH endpoint qnames AND the total inferred-edge count (`toBe(N)`); assert `confidence:'inferred'` + numeric score; assert NO `parsed`/`declared` edge, `stubCount:0`, no self-edge. Seed `golden-e2e.json`; byte-identical re-run. Then inspect the persisted `.db` for the sentinel fixture value → assert it is ABSENT. Spec: Pipeline pins inferred edges by exact endpoints and count; No fixture value reaches the persisted index. Done: `DBGRAPH_INTEGRATION=1 npm run test:integration` green; goldens seeded + byte-identical.
- [ ] 7.6 GREEN `package.json`: add `mongodb` to `optionalDependencies` (ADR-002/006; justify in commit body); `testcontainers` already present; `npm ci` succeeds. Spec: `mongodb` is the only new optional runtime dep. Done: `mongodb` under `optionalDependencies`; `npm ci` clean.
- [ ] 7.7 GREEN `.github/workflows/ci.yml`: KEEP the existing `test` + `mssql-integration` + `pg-integration` + `mysql-integration` jobs unchanged; ADD a separate gated `mongodb-integration` job (Linux-only, `needs: []`) setting `DBGRAPH_INTEGRATION=1`, running ONLY `npm run test:integration` (or the mongodb subset), using ONLY an ephemeral `mongo:7` container, never required by the unit matrix. Spec: mongodb CI job is gated and never blocks the unit matrix. Done: YAML valid; unit matrix does NOT depend on `mongodb-integration`.
- [ ] 7.8 Final GATE + closeout: `npx tsc --noEmit` clean; `npm run lint` 0/0; `npm test` green incl. `security-scan.test.ts` over `engines/**`; `git diff --exit-code test/golden/normalize/` STILL empty (SQL goldens never drifted across the whole change); `DBGRAPH_INTEGRATION=1 npm run test:integration` green against a REAL `mongo:7`. Golden files seeded and byte-identical. Spec: write-verb scanner stays green; CI green including the gated mongodb-integration job. Done: all gates green.

## Apply Batch Grouping (one sub-agent session each)

- **Batch 1** (1.1–1.4): `RawField`+`FieldPayload`+`RawObject.fields?`, normalizer `buildFieldNode`+`fields` branch, PROVE SQL normalize goldens byte-identical — NO Mongo code, NO DB.
- **Batch 2** (2.1–2.6): `MONGODB_CAPABILITIES`, `MongodbAdapterConfig` union widening, `MongodbSource` config + parse-config + resolve-secrets `mongodb` branch — pure logic, NO connection.
- **Batch 3** (3.1–3.5): driver seam, error-mapper, factory (lazy `import('mongodb')`), probe, adapter skeleton — pure/synthetic, NO DB.
- **Batch 4** (4.1–4.5): `type-map.ts`, sampling+type-merge+DISCARD walk, `map.ts`, adapter `extract`/`fingerprint` fill, US-031 scanner confirm — pure logic over fake cursors, NO DB.
- **Batch 5** (5.1–5.5): dispatch wiring (`open-connections`, `index.ts` barrel/`capabilitiesFor`), pinned message + code-4 guard, inference-on unit test.
- **Batch 6** (6.1–6.3): `docs/permissions/mongodb.md`, `PermissionError` doc-link verify, US-030/031/033 story reconciliation.
- **Batch 7** (7.1–7.8): torture seed + Testcontainers harness, RawCatalog golden + values-never-persisted + fingerprint, inference + E2E goldens, `mongodb` optionalDependency, gated `mongodb-integration` CI, final scanner/lint/golden-diff closeout — Docker-gated critical path.

### Dependency bottlenecks

- **Batch 1 gates EVERYTHING.** `RawField`/`FieldPayload` (1.1/1.2) + the `buildFieldNode` branch (1.3) must land AND the SQL normalize goldens must stay byte-identical (1.4) before any Mongo `map.ts` (4.3) emits `fields` or any `'field'` node is normalized. A SQL golden drift here is a HARD STOP (the no-CI safety net). `dataType` MUST be a STRING (not `types[]`) or `inferReferences` cannot consume the `field` node.
- **2.2 (union widening) gates all Mongo modules** referencing `MongodbAdapterConfig` (3.3 factory, 3.5/4.4 adapter, 5.1 wiring); the prior-phase green check (2.3) must pass before wiring. **2.6 (resolve-secrets) is easy to forget** — the URI env-ref is unresolved without it and 5.1's wiring would pass a literal `${env:VAR}`.
- **3.1 (driver seam) gates 3.3** (factory wraps it), **3.5/4.4** (adapter talks ONLY to it) and **4.x** (walk consumes its `sample()` output); **3.2 (error-mapper) gates 3.3** (factory maps connect errors) and **6.2** (doc-link).
- **4.1 type-map + 4.2 sample-walk gate 4.3 map** (it composes them) and **4.4 adapter** (wires them); the DISCARD invariant (4.2) is the load-bearing security test and must be GREEN before any integration run.
- **5.3 message + 5.4 code-4 guard are atomic** — they MUST land in the same batch as one contract; splitting them risks a red pinned assertion or an unverified exit-code mapping.
- **7.1 torture.ts + 7.2 container gate ALL integration (7.3–7.5)** — the Docker-dependent critical path. The exact `mongo:7` shapes (the precise `$jsonSchema`/index shapes, the sometimes-present field frequency) and the `fingerprint` behavior are FINALIZED by 7.3/7.4 against the live container, so 4.4's adapter is provisional until pinned. `$sample(size ≥ doc_count)` is MANDATORY for the deterministic golden (ADR-008).
- **7.6 (`mongodb` optionalDependency) + the `test:integration` script** must be in place before 7.7's CI job and the 7.3–7.5 done-checks can run cleanly.

## Definition of Done (tied to the proposal's Success Criteria)

- [ ] `MongodbSchemaAdapter` implements the EXISTING `SchemaAdapter` port (no port SHAPE change), wired via `createMongodbSchemaAdapter` through the composition root (`open-connections.ts` + `src/index.ts`). — Batches 2, 3, 5
- [ ] `mongodb` extracts collections (kind `collection`, schema = database), indexes (unique/compound, `_id` once), and top-level `$jsonSchema` in `extra`; fields as dotted/array-encoded paths with UNION dataType + presence frequency. — Batches 1, 4, 7
- [ ] Sampled VALUES are DISCARDED after the type-merge and NEVER reach the `RawCatalog` or the persisted `.db` (asserted by a sentinel-value test); the write-verb scanner stays green over `src/adapters/engines/**`. — Batches 4, 7
- [ ] `inferReferences` fires AUTOMATICALLY from the `collection`/`field` node presence (NO infer-engine change); `<entity>_id` → `<collection>._id` `inferred_reference` edges with EXACT endpoint qnames + count + numeric score; NO declared/parsed edge for Mongo. — Batches 1, 5, 7
- [ ] The additive `RawField`/`RawObject.fields?` + `buildFieldNode` branch leave the SQL normalize goldens BYTE-IDENTICAL (`git diff --exit-code test/golden/normalize/` empty) — the no-CI safety net. — Batches 1, 7
- [ ] `MONGODB_CAPABILITIES` truthful: `collection`/`field`/`index` supported; `table/column/constraint/view/procedure/function/trigger/sequence` UNSUPPORTED; `supportsBodies:false`; `supportsDependencyHints:false`; 100% matrix coverage by the fixture. — Batches 2, 7
- [ ] Connectivity via a `${env:VAR}` URI + database (+ optional `sampleSize?`/`tls?`); NO `schema?`/host/port knobs; absent `mongodb` raises an error naming `npm i mongodb`; URI is env-only (literal rejected). — Batches 2, 3
- [ ] `docs/permissions/mongodb.md` ships the minimal read-only role (`read` on the target DB; `dbStats`/`listCollections`/`listIndexes`/`find` privileges; NO write/admin); a missing privilege raises an actionable `PermissionError` naming the privilege + the doc. — Batches 3, 6
- [ ] `fingerprint()` derives from `dbStats` → `sha256(collections|indexes|objects)`; CHANGES on collection/index DDL, STABLE on data-only changes; does NOT walk documents. — Batches 4, 7
- [ ] Determinism (ADR-008): the `RawCatalog` is golden-pinned and byte-identical via the FIXED dataset + `$sample(size ≥ doc_count)`; the E2E pins exact inferred-edge endpoints AND count, `stubCount:0`, no self-edge. — Batch 7
- [ ] The pinned `UnsupportedDialectError` message updated to `sqlite, mssql, pg, mysql, mongodb` with its assertion AND the exit-code-4 mapping verified unchanged (via `instanceof` guard, `exit-code.ts` untouched). — Batch 5
- [ ] `mongodb` is the ONLY new optional runtime dep; `tsc`/lint/test clean; CI green including the gated `mongodb-integration` job (`DBGRAPH_INTEGRATION=1`), which never blocks the unit matrix. — Batch 7
- [ ] US-030 refined with the locked decisions encoded; US-031 (values-never-persisted, read-only) + US-033 (permission doc) reconciled in the stories. — Batch 6
