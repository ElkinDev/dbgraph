# Tasks: Phase 8.5 — Resilient Connectivity (engine-agnostic probe, graceful degradation, adaptive profiles)

Standing header (every task): STRICT TDD (RED→GREEN→refactor; the failing test PRECEDES the code; NEVER mock a
native driver — inject the `import()`/`spawnSync` seam instead). Pure units (`ConnectivityOutcome` builders,
`resolveProfile`, `reassembleForJson`, `formatOutcome`, `formatDoctor`, per-engine `probe`) use injected seams +
string/buffer fixtures — NO DB. The seam is a TYPED THROW carrying the outcome (Design Decision §"ConnectivityOutcome
seam — typed throw, not return-type change"): the per-engine factory throws `ConnectivityUnavailableError(outcome)`
caught at the EXISTING `cli.ts` `catch (err instanceof DbgraphError)` boundary (lines 74-81) and rendered by the NEW
pure `src/core/present/connectivity.ts`. The happy-path `SchemaAdapter` return type is UNCHANGED — the success branch
of every factory (`createPgSchemaAdapter`/`createMysqlSchemaAdapter` returning a connected adapter) and its tests are
UNTOUCHED; the outcome path is reached ONLY on driver-absent / connect-failure / strategy-exhaustion. `ConnectivityOutcome`
+ `ConnectivityOption` + `ConnectivityUnavailableError` are CORE types (driver-free, no `child_process`), referenced by
BOTH the `connectivity-diagnostics` and `connectivity` specs — those two specs are the AUTHORITATIVE contract; engine
adapters WIRE to them (do NOT author separate per-engine extraction-spec deltas). Honor: hexagonal ADR-004 (core imports
nothing outward; `probe()` on the port stays driver-free; `child_process` shelling lives ONLY in adapters and is NOT a
native driver), determinism ADR-008 (the profile affects TRANSPORT only, never catalog CONTENT — mssql goldens stay
byte-identical), strict TS / no `any` / `exactOptionalPropertyTypes` (conditional spread). Cross-platform `path.win32`
/`path.posix` — the PATH probe (`where`/`which` for `sqlcmd`/`psql`/`mysql`) works on Windows AND Linux, no hard-coded
separators. ZERO new npm deps (probing/detection stay on `node:child_process` + dynamic `import()`; ADR-006/007). Reuse
the SHIPPED `StrategyExhaustionError`/`ConnectionError`/`PermissionError` — do NOT redefine them. Conventional commits
referencing US-039..044; ENGLISH. EXACT-set assertions from the start (L-009): `toEqual` the FULL options array shape,
`toBe(N)` counts, explicit no-leak (`not.toContain`) negative assertions — existence-only `find().toBeDefined()` is
FORBIDDEN. The run-it-yourself option's queries MUST be the EXACT read-only catalog `SELECT`s already shipped in each
adapter (`queries.ts`) surfaced as paste-able strings, asserted write-verb-free.

Per-batch GATE (the batch's exit criteria, ALL must pass before the next batch): `npx tsc --noEmit` clean ·
`npm run lint` 0 errors / 0 warnings · `npm test` green INCLUDING the write-verb scanner
`test/adapters/engines/security-scan.test.ts` over `engines/**` · plus the batch-specific proof noted in each section.
Batch 3 additionally re-proves the pg + mysql HAPPY-CONNECT suites UNCHANGED (the new path must not perturb success).
Batch 4 additionally proves the mssql `RawCatalog` + E2E goldens (`test/fixtures/mssql/golden/golden-raw-catalog.json`
+ `golden-e2e.json`) BYTE-IDENTICAL after the reassembly extraction — a golden drift here is a hard STOP (mirrors 8b's
`_shared` promotion gate). Batch 6 additionally lands the OPT-IN `sqlcmd` CI lane that NEVER blocks the unit matrix.

## Batch 1: Core types — `ConnectivityOutcome`/options/`ConnectivityUnavailableError`, `CapabilityProbe` port, optional `probe?()`, the `present/connectivity.ts` renderer, barrels (driver-free, NO adapters)

> Satisfies the type half of `connectivity-diagnostics` "Connection failure yields a typed non-blocking outcome
> presenting at least three options" + "Probe port stays driver-free and core-typed", and the `connectivity` MODIFIED
> "Exhausting all strategies surfaces a typed, redacted, actionable outcome". US-041 (types) + US-039 (port shape).
> Design Decisions §"ConnectivityOutcome + options live in core" and §"probe() is OPTIONAL on the strategy port".
> NO adapter or CLI code in this batch — pure core types + a pure formatter. Everything here is driver-free (ADR-004).

- [x] 1.1 RED→GREEN `test/core/ports/capability-probe.test.ts` (type-level + an import-surface assertion) + `src/core/ports/capability-probe.ts`: define `CliToolInfo` (`{ readonly tool: string; readonly version: string | null; readonly path: string | null }`), `ProbeResult` (`{ readonly nativeDriver: boolean; readonly cliTools: readonly CliToolInfo[]; readonly odbc: boolean }`), `CapabilityProbe` (`{ readonly engine: string; probe(): Promise<ProbeResult> }` — MUST NOT throw, MUST NOT open a DB connection). Imports ONLY core types — NO driver, external-tool, or `node:child_process` symbol. Assert (via a source-text/import scan in the test, mirroring the connectivity-strategy port discipline) the module imports no `child_process`/driver symbol. Spec: Probe port stays driver-free and core-typed. Done: `npx tsc --noEmit`; import-surface test green.
- [x] 1.2 RED→GREEN `test/core/errors-connectivity.test.ts` + `src/core/errors.ts` (ADD ONLY — do NOT touch existing classes): add the `ConnectivityOption` discriminated union (`{ kind: 'run-it-yourself'; description: string; queries: readonly string[] } | { kind: 'consented-install'; description: string; tool: string; docUrl: string } | { kind: 'manual-dump'; description: string; outputPath: string }`), the `ConnectivityOutcome` interface (`{ readonly engine: string; readonly summary: string; readonly attempts: readonly StrategyAttempt[]; readonly options: readonly ConnectivityOption[] }` — reuse the EXISTING `StrategyAttempt` type-only import already present at errors.ts line 116), and `class ConnectivityUnavailableError extends DbgraphError` with `code: 'E_CONNECTIVITY_UNAVAILABLE'` carrying `readonly outcome: ConnectivityOutcome`. Assert: `instanceof DbgraphError` TRUE; `code === 'E_CONNECTIVITY_UNAVAILABLE'`; `name === 'ConnectivityUnavailableError'`; `outcome.options.length >= 3` for a constructed sample; the message is content-free (carries `outcome.summary`, no identifier). Spec: the typed non-blocking outcome is a core type; `StrategyExhaustionError`/`ConnectionError`/`PermissionError` are UNCHANGED. Done: `npm test errors-connectivity`.
- [x] 1.3 RED→GREEN `test/core/present/connectivity.test.ts` + `src/core/present/connectivity.ts`: pure `formatOutcome(outcome: ConnectivityOutcome): string` (mirror `present/status.ts` SHAPE — imports ONLY core types). Renders the engine, the content-free summary, EACH attempt (`id — reason`), and ALL ≥3 options: run-it-yourself prints its exact paste-able queries verbatim, consented-install prints the tool + docUrl behind a CONSENT notice (no auto-install), manual-dump prints the outputPath. EXACT-set assertions: a fixture outcome with all three option kinds renders a string CONTAINING each option's marker text AND each verbatim query; `not.toContain` any raw `Error:`/stack-frame text; `not.toContain` a schema/identifier sample injected into the fixture's queries-adjacent fields (proves the renderer surfaces ONLY the option payload, content-free except the explicit queries). Spec: run-it-yourself option carries exact read-only catalog queries; consented-install never installs without explicit consent. Done: `npm test present/connectivity`.
- [x] 1.4 RED→GREEN extend `test/core/ports/connectivity-strategy.test.ts` (or add it) + `src/core/ports/connectivity-strategy.ts`: add OPTIONAL `probe?(): Promise<ProbeResult>` to the `ConnectivityStrategy` interface (back-compat — existing strategies that omit it still satisfy the port). Import `ProbeResult` type-only from `capability-probe.js`. Assert a shipped strategy stub WITHOUT `probe` still type-checks as `ConnectivityStrategy`. Spec: probe is non-throwing and optional on the contract; the port stays driver-free. Done: `npx tsc --noEmit`; existing connectivity-strategy + registry suites UNCHANGED.
- [x] 1.5 GREEN `src/core/index.ts` barrel (ADD ONLY): export `type CapabilityProbe`, `type ProbeResult`, `type CliToolInfo` (after the connectivity-strategy port block, lines ~89-93); export `type ConnectivityOption`, `type ConnectivityOutcome`, `ConnectivityUnavailableError` (in the error block, lines ~163-175); export `formatOutcome` (in the present block, lines ~106-155). Spec: core public surface exposes the new types/error/formatter/port. Done: `npx tsc --noEmit`; `npm test` (barrel-shape suite, if present, green).
- [ ] 1.6 GATE — `npx tsc --noEmit` clean · `npm run lint` 0/0 · `npm test` green incl. `security-scan.test.ts`. Prove ADR-004: the new port + present module import NO driver/`child_process` symbol (the 1.1 import-surface test stands). Done: all gates green; NO adapter/CLI file changed in this batch.

## Batch 2: Probe seam — per-engine `probe.ts` for mssql/pg/mysql/sqlite (driver-present via injected `import()`; PATH scan cross-platform; NON-throwing)

> Satisfies `connectivity-diagnostics` "Engine-agnostic capability probe reports available methods without raising"
> (all four scenarios: present driver+CLI; absent both; timed-out/failed → unavailable; port stays driver-free).
> US-039. Design Decision §"probe() is OPTIONAL on the strategy port; per-engine probe in adapters". Detection is
> environment I/O → it lives in adapters; the SHAPE is the core-typed `ProbeResult` from Batch 1. Dynamic
> `import('pg'/'mysql2'/'tedious')` checks driver PRESENCE only — it MUST NOT call `.connect()`/open a connection.
> The PATH scan mirrors the SHIPPED `SqlcmdStrategy.detect()` pattern (`where`/`which`, `shell: false`, short timeout).

- [ ] 2.1 RED→GREEN `test/adapters/engines/mssql/probe.test.ts` + `src/adapters/engines/mssql/probe.ts`: `MssqlCapabilityProbe implements CapabilityProbe` (`engine = 'mssql'`). Inject a `spawnSync` seam (mirror `SqlcmdStrategy`'s constructor seam) AND a driver-import seam (`importTedious?: () => Promise<unknown>`). `probe()` resolves `nativeDriver` from whether the tedious import RESOLVES (no `.connect()`), `cliTools` from a `where sqlcmd`/`which sqlcmd` scan (capture version via `sqlcmd -?` parse → `version: string | null`, `path: string | null`), `odbc` from the SHIPPED `OdbcDriverStrategy.detect()` registry probe. EXACT-set assertions: driver-present + sqlcmd-on-PATH → `{ nativeDriver: true, cliTools: [{ tool: 'sqlcmd', version: <parsed>, path: <parsed> }], odbc: <bool> }`; driver-absent (import rejects MODULE_NOT_FOUND) + sqlcmd-absent (`where` non-zero) → `{ nativeDriver: false, cliTools: [{ tool: 'sqlcmd', version: null, path: null }], odbc: false }`; a TIMED-OUT `spawnSync` (error set) → that tool unavailable, NO throw. Assert `probe()` NEVER rejects (wrap an injected seam that throws → resolves a negative result). Spec: Probe reports a present native driver and a CLI on PATH; Absent driver and absent CLI are reported, not raised; A timed-out or failed detection is treated as unavailable. Done: `npm test mssql/probe`.
- [ ] 2.2 RED→GREEN `test/adapters/engines/pg/probe.test.ts` + `src/adapters/engines/pg/probe.ts`: `PgCapabilityProbe` (`engine = 'pg'`); inject `importPg?: () => Promise<unknown>` (mirror the factory's `importPg` seam) + `spawnSync`. `nativeDriver` from the `pg` import resolving (NO `Client.connect()`); `cliTools` from a `psql` PATH scan; `odbc: false` (N/A for pg). EXACT-set: pg-present + psql-present → both available; pg-absent + psql-absent → both unavailable, no throw. Spec: Probe reports a present native driver and a CLI on PATH; Absent driver and absent CLI are reported, not raised. Done: `npm test pg/probe`.
- [ ] 2.3 RED→GREEN `test/adapters/engines/mysql/probe.test.ts` + `src/adapters/engines/mysql/probe.ts`: `MysqlCapabilityProbe` (`engine = 'mysql'`); inject `importMysql?: () => Promise<unknown>` (mirror the factory seam) + `spawnSync`. `nativeDriver` from the `mysql2` import resolving; `cliTools` from a `mysql` PATH scan; `odbc: false`. EXACT-set: mysql2-present + mysql-CLI-present → both available; both absent → both unavailable, no throw. Spec: same probe scenarios. Done: `npm test mysql/probe`.
- [ ] 2.4 RED→GREEN `test/adapters/engines/sqlite/probe.test.ts` + `src/adapters/engines/sqlite/probe.ts`: `SqliteCapabilityProbe` (`engine = 'sqlite'`); `nativeDriver` from the `node:sqlite` / `better-sqlite3` availability check (the SHIPPED sqlite driver-selection logic), `cliTools: []` (no separate CLI tool relevant), `odbc: false`. Keeps the probe surface uniform across all four engines so `doctor` is engine-agnostic. Spec: the probe reports availability per engine without raising. Done: `npm test sqlite/probe`.
- [ ] 2.5 Confirm US-031 scanner stays green over the new `probe.ts` files: run `test/adapters/engines/security-scan.test.ts` — the `where`/`which`/`sqlcmd -?`/`psql`/`mysql` argv literals carry NO write verb; no quoted SQL example in `probe.ts` JSDoc triggers a false positive. Spec: probe modules pass the engines write-verb scanner (US-031). Done: `npm test security-scan`.
- [ ] 2.6 GATE — `npx tsc --noEmit` · `npm run lint` 0/0 · `npm test` incl. `security-scan.test.ts`. Prove cross-platform: the PATH-scan unit asserts BOTH a `process.platform === 'win32'` (`where`) and a POSIX (`which`) branch via the injected `spawnSync` (no hard-coded separator). Done: all gates green.

## Batch 3: Outcome wiring — pg/mysql factories (driver-absent/connect-fail → outcome) + mssql `selectStrategy` exhaustion → outcome; `cli.ts` catch renders; `cli/format/exhaustion.ts` shim; ENGINE-AGNOSTIC PARITY tests

> Satisfies `connectivity-diagnostics` "Connection failure yields a typed non-blocking outcome presenting at least
> three options" (pg-absent + mssql-exhaustion + run-it-yourself + consented-install scenarios) and `connectivity`
> MODIFIED "Exhausting all strategies surfaces a typed, redacted, actionable outcome" (all three scenarios). US-041
> (engine-agnostic) + the redacted-reason half of US-041. Design Decision §"typed throw, not return-type change" +
> §"the legacy `cli/format/exhaustion.ts` becomes a thin shim". THE HAPPY-CONNECT BRANCH OF BOTH FACTORIES IS
> UNTOUCHED. This batch PROVES parity: a pg/mysql driver-absent case AND an mssql strategy-exhaustion case yield the
> SAME ≥3 options. The run-it-yourself queries are the EXACT shipped catalog SELECTs, asserted write-verb-free.

- [ ] 3.1 RED→GREEN `test/adapters/engines/_shared/connectivity-outcome.test.ts` + `src/adapters/engines/_shared/connectivity-outcome.ts`: an engine-agnostic helper `buildConnectivityOutcome({ engine, summary, attempts, runItYourselfQueries, installTool, installDocUrl, dumpPath }): ConnectivityOutcome` that assembles the THREE options identically for every engine (run-it-yourself carries the passed exact catalog SELECTs; consented-install carries tool+docUrl; manual-dump carries the dump path). EXACT-set: returns `options.length === 3` with kinds `['run-it-yourself','consented-install','manual-dump']` in that order; the run-it-yourself `queries` array EQUALS the passed SELECTs; `not.toContain` any write verb across all queries (regex over `INSERT|UPDATE|DELETE|MERGE|CREATE|ALTER|DROP|TRUNCATE`, case-insensitive). This is the SINGLE source of the ≥3-options shape — both factories and the mssql registry call it (engine-agnostic by construction, satisfying the parity requirement). Spec: the SAME three options for any engine; run-it-yourself carries write-verb-free catalog SELECTs. Done: `npm test connectivity-outcome`.
- [ ] 3.2 RED→GREEN extend `test/adapters/engines/pg/factory.test.ts` + `src/adapters/engines/pg/factory.ts`: REPLACE the MODULE_NOT_FOUND `throw new ConnectionError("... npm i pg")` (lines ~94-97) AND the `throw mapPgError(cause)` connect-fail path (line ~137) so that — instead of a bare throw — the factory builds an outcome via `buildConnectivityOutcome` (engine `'pg'`; run-it-yourself = the EXACT shipped pg catalog SELECTs from `pg/queries.ts`; consented-install tool `'pg'` + the npm/pg docUrl; manual-dump the pg dump path) and throws `ConnectivityUnavailableError(outcome)`. The HAPPY path (successful `client.connect()` → `new PgSchemaAdapter`) is UNCHANGED — keep its tests green. EXACT-set: driver-absent (`importPg` rejects MODULE_NOT_FOUND) → caught error is `ConnectivityUnavailableError` with `outcome.options.length === 3`, run-it-yourself queries EQUAL the pg catalog SELECTs, write-verb-free; the install option still names `pg`. Spec: pg driver absent yields the three-option outcome, not a bare throw. Done: `npm test pg/factory`.
- [ ] 3.3 RED→GREEN extend `test/adapters/engines/mysql/factory.test.ts` + `src/adapters/engines/mysql/factory.ts`: same transform as 3.2 for mysql — MODULE_NOT_FOUND (lines ~100-103) AND `mapMysqlError(cause)` connect-fail (line ~146) build a `buildConnectivityOutcome` (engine `'mysql'`; run-it-yourself = the EXACT shipped mysql catalog SELECTs from `mysql/queries.ts`; install names `mysql2`; manual-dump the mysql dump path) and throw `ConnectivityUnavailableError(outcome)`. HAPPY connect path UNTOUCHED. EXACT-set: driver-absent → `ConnectivityUnavailableError`, 3 options, mysql catalog SELECTs verbatim + write-verb-free, install names `mysql2`. Spec: pg/mysql driver-absent yields the engine-agnostic three-option outcome. Done: `npm test mysql/factory`.
- [ ] 3.4 RED→GREEN extend `test/adapters/engines/mssql/strategies/registry.test.ts` + `src/adapters/engines/mssql/strategies/registry.ts`: at the exhaustion point in `selectStrategy` (currently `throw new StrategyExhaustionError(attempts)`, line ~182), build a `buildConnectivityOutcome` (engine `'mssql'`; `attempts` = the collected `StrategyAttempt[]`; run-it-yourself = the EXACT shipped mssql catalog SELECTs from `queries.ts`; consented-install tool `'sqlcmd'` + the official Microsoft docUrl already in `exhaustion.ts`; manual-dump = the SHIPPED `.dbgraph/dumps/mssql-dump.json` path) and throw `ConnectivityUnavailableError(outcome)` INSTEAD. Keep `StrategyExhaustionError` exported/usable (do NOT delete it — it remains the typed redacted reason carrier; the outcome's `attempts` mirror it). EXACT-set: an all-skipped chain → caught `ConnectivityUnavailableError` whose `outcome.attempts` lists EACH strategy + reason (native skipped, sqlcmd absent, manual-dump absent, install not consented) AND whose `options.length === 3` MATCH the pg/mysql option kinds/order. Spec: mssql strategy exhaustion yields the same three-option outcome; All strategies exhausted lists each attempt and reason. Done: `npm test registry`.
- [ ] 3.5 RED→GREEN ENGINE-AGNOSTIC PARITY suite `test/adapters/engines/_shared/parity.test.ts`: drive a pg driver-absent outcome (via the 3.2 path), a mysql driver-absent outcome (3.3), AND an mssql strategy-exhaustion outcome (3.4); assert all THREE yield `options.map(o => o.kind)` EQUAL `['run-it-yourself','consented-install','manual-dump']`, each `options.length === 3`, and each run-it-yourself option is write-verb-free. Assert the parity is PROVEN, not asserted by inspection: the three outcomes share the identical option-kind set. Spec: the SAME at-least-three options whether pg/mysql (driver absent) or mssql (exhausted). Done: `npm test parity`.
- [ ] 3.6 RED→GREEN extend `test/cli/cli.test.ts` + `src/cli/cli.ts`: in the `catch (err instanceof DbgraphError)` block (lines 74-81), BEFORE the generic `Error: ${err.message}` write, add `if (err instanceof ConnectivityUnavailableError) { process.stderr.write(formatOutcome(err.outcome) + '\n'); return exitCodeFor(err); }` (import `ConnectivityUnavailableError` + `formatOutcome` from the barrel). EXACT-set: a thrown `ConnectivityUnavailableError` renders the ≥3 options to stderr; the output `not.toContain` a stack frame / `at ` line; the exit code maps via the existing `exitCodeFor` (add `E_CONNECTIVITY_UNAVAILABLE` to `src/cli/exit-code.ts` mapping if it does not already fall through to a non-zero default — assert the chosen code). Spec: NO raw exception or stack trace reaches the user. Done: `npm test cli`.
- [ ] 3.7 RED→GREEN `test/cli/format/exhaustion.test.ts` (extend) + `src/cli/format/exhaustion.ts`: convert `formatExhaustionError` into a thin SHIM that builds a `ConnectivityOutcome` from the `StrategyExhaustionError.attempts` and DELEGATES to core `formatOutcome` (import from the barrel). The EXISTING exhaustion test must stay green (the two SHIPPED options — manual-dump + guided-install — are now a subset of the ≥3 rendered; update the assertion to the new ≥3-option output without weakening it). Spec: the legacy presenter delegates to the engine-agnostic renderer; no behavior regression. Done: `npm test exhaustion`.
- [ ] 3.8 GATE — `npx tsc --noEmit` · `npm run lint` 0/0 · `npm test` incl. `security-scan.test.ts`. ADDITIONALLY re-prove the pg + mysql HAPPY-CONNECT suites UNCHANGED (the success branch + `PgSchemaAdapter`/`MysqlSchemaAdapter` construction tests pass byte-for-byte — the new path is reached ONLY on driver-absent/connect-fail). The parity suite (3.5) is green. Done: all gates green; happy paths untouched.

## Batch 4: Profile registry + reassembly extraction — `profiles.ts` (seeded legacy-15.x), `sqlcmd.strategy.probe()`, extract `reassembleForJson` to `json-rows.ts` (mssql BYTE-IDENTICAL gate)

> Satisfies `connectivity` ADDED "Variant/version profile registry encodes known quirks as data" (all three
> scenarios) + "Profile-driven reassembly of chunked output is hardened and golden-pinned" (all four scenarios).
> US-040 + US-042. Design Decisions §"SqlcmdProfile registry is a data table in adapters" and §"reassembly extracted
> to profile-driven `json-rows.ts`". The reassembly logic currently PRIVATE in `sqlcmd.strategy.ts`
> (`extractJsonContent`/`reassembleJsonOutput`/`reassembleSingleObjectOutput`, lines 133-227) MOVES into `json-rows.ts`
> as exported `reassembleForJson(stdout, profile)`; `parseJsonRows` (the coercion layer) is UNCHANGED. The shipped
> hard-coded flags (`-y 0 -f o:65001`, no `-h`/`-W` — F-3) + the 2033-char/no-header assumptions (F-4) become the
> seeded legacy-15.x profile entry. THE mssql GOLDENS + GATED INTEGRATION MUST STAY BYTE-IDENTICAL — a hard STOP gate.

- [ ] 4.1 RED→GREEN `test/adapters/engines/mssql/strategies/profiles.test.ts` + `src/adapters/engines/mssql/strategies/profiles.ts`: export `SqlcmdProfile` (`{ readonly variant: string; readonly versionRange: string; readonly flags: readonly string[]; readonly outputShape: { chunkSize: number; hasHeader: boolean }; readonly encoding: string }`), a keyed `SQLCMD_PROFILES: readonly SqlcmdProfile[]` seeded with the legacy-15.x entry (flags `['-y','0','-f','o:65001']` — `-y 0` ALONE, NO `-h`/`-W` per F-3; `outputShape: { chunkSize: 2033, hasHeader: false }` per F-4/F-6; `encoding: 'utf8'` from `-f o:65001` per F-5), and `resolveProfile(probe: ProbeResult): SqlcmdProfile` returning the legacy-15.x entry for a matching sqlcmd version and a CONSERVATIVE DEFAULT (never crash) on a miss. EXACT-set: a probe identifying sqlcmd 15.x → the legacy entry with flags `toEqual(['-y','0','-f','o:65001'])`, `chunkSize === 2033`, `hasHeader === false`, `encoding === 'utf8'`; a probe matching NO entry → the default profile, NO throw; the legacy entry is a DATA row (assert it exists in `SQLCMD_PROFILES`, not a code branch). Spec: Legacy sqlcmd 15.x quirks resolve from a registry entry; A new environment is added as a profile entry, not a patch; An unrecognized probe result yields a conservative default profile. Done: `npm test profiles`.
- [ ] 4.2 RED→GREEN `test/adapters/engines/mssql/strategies/json-rows-reassembly.test.ts` + `src/adapters/engines/mssql/strategies/json-rows.ts`: MOVE the private `extractJsonContent`/`reassembleJsonOutput`/`reassembleSingleObjectOutput` out of `sqlcmd.strategy.ts` INTO `json-rows.ts` as exported `reassembleForJson(stdout: Buffer, profile: SqlcmdProfile): unknown[]` + `reassembleSingleForJson(stdout, profile): Record<string, unknown>` driven by the profile's `chunkSize`/`hasHeader`/`encoding` (decode using `profile.encoding`; skip a leading non-JSON line + the `(N rows affected)` trailer + blank lines; concatenate VERBATIM — NEVER `.trim()` at a chunk boundary per F-4/F-6; coerce via the UNCHANGED `parseJsonRows`/`coerceStringy` for `sql_variant`→JSON per F-9). On malformed/partial output throw a TYPED actionable error (what was received, first N chars) — NOT a raw `JSON.parse` stack. `parseJsonRows` SHAPE UNCHANGED. EXACT-set goldens over the F-4/F-6 fixtures (added in Batch 6, or seed minimal inline buffers here): a 2033-char chunked no-header buffer reassembles to the EXACT golden array (full-set, byte-identical on re-run — L-009/ADR-008); a non-UTF-8 buffer decodes per `encoding` preserving non-ASCII; a truncated buffer → typed error reporting first N chars, no stack. Spec: Chunked FOR JSON output reassembles without trimming; Non-UTF-8 output is decoded per the profile encoding; Malformed output yields a typed actionable error, not a stack trace; Reassembly is golden-pinned with exact-set assertions. Done: `npm test json-rows-reassembly`.
- [ ] 4.3 RED→GREEN extend `test/adapters/engines/mssql/strategies/sqlcmd.strategy.test.ts` + `src/adapters/engines/mssql/strategies/sqlcmd.strategy.ts`: add `probe(): Promise<ProbeResult>` (delegating to `MssqlCapabilityProbe` from Batch 2 + a tiny `SELECT ... FOR JSON` round-trip measuring shape/encoding — content-free, READ-ONLY); thread the resolved `SqlcmdProfile` (from `resolveProfile`) into `runCatalog`/`fingerprint` so flags come from `profile.flags` and reassembly calls the EXTRACTED `reassembleForJson(result.stdout, profile)` / `reassembleSingleForJson` (replacing the now-removed private functions). The DEFAULT profile reproduces the SHIPPED flags exactly (`-y 0 -f o:65001`). EXACT-set: with the legacy-15.x profile, the spawned argv `toEqual` the SHIPPED `['-E','-S',...,'-y','0','-f','o:65001']` (no `-h`/`-W`); `runCatalog` over a recorded stdout fixture yields the SAME `RawCatalog` as before extraction. Spec: the profile is selected by the probe, never assumed; transport flags come from the profile data. Done: `npm test sqlcmd.strategy`.
- [ ] 4.4 GATE — BYTE-IDENTICAL (hard STOP): `npx tsc --noEmit` · `npm run lint` 0/0 · `npm test` incl. `security-scan.test.ts` AND the full mssql unit suite. THEN re-assert `test/fixtures/mssql/golden/golden-raw-catalog.json` + `golden-e2e.json` UNCHANGED, AND run the gated mssql integration (`DBGRAPH_INTEGRATION=1 npm run test:integration` against the mssql lane) BYTE-IDENTICAL. CHANGING any mssql golden here is a STOP — the profile/reassembly extraction drifted behavior; investigate, do NOT re-bless. Spec: Determinism (ADR-008) — the profile affects TRANSPORT only; mssql goldens byte-identical. Done: every mssql suite + golden passes UNCHANGED; the round-trip is determinism-preserving.

## Batch 5: `dbgraph doctor` command — `present/doctor.ts` (content-free), `runDoctor`, `handleDoctor` + `COMMAND_TABLE['doctor']` + USAGE; leak assertion

> Satisfies `connectivity-diagnostics` "dbgraph doctor reports diagnostics content-free" (all three scenarios).
> US-043. Design Decision §"`dbgraph doctor` is a new content-free CLI command". Runs `CapabilityProbe` stand-alone
> (NO DB connection, NO catalog SELECT), formats via a PURE `present/doctor.ts`. Output: engine, native-driver bool,
> CLI tools + versions, ODBC bool, resolved profile NAME, chosen-strategy id — SHAPE ONLY, ZERO schema/identifier/secret.
> NON-throwing: an unrecognized environment produces a report, not an exception.

- [ ] 5.1 RED→GREEN `test/core/present/doctor.test.ts` + `src/core/present/doctor.ts`: define `DoctorView` (`{ readonly engine: string; readonly nativeDriver: boolean; readonly cliTools: readonly CliToolInfo[]; readonly odbc: boolean; readonly resolvedProfile: string; readonly chosenStrategy: string }`) + pure `formatDoctor(view: DoctorView): string` (mirror `present/status.ts` content-free SHAPE — imports ONLY core types). EXACT-set + LEAK assertions: a view built from a fixture WITH a planted schema name, object identifier, and secret value in adjacent (non-view) context → the rendered string `not.toContain` ANY of those planted strings; it DOES contain the engine, the driver bool, each tool name + version, the profile name, the strategy id. Export `formatDoctor` + `type DoctorView` from the barrel. Spec: doctor reports capability, chosen strategy and environment profile; doctor output is content-free and safe to share. Done: `npm test present/doctor`.
- [ ] 5.2 RED→GREEN `test/cli/commands/doctor.test.ts` + `src/cli/commands/doctor.ts`: `runDoctor(deps)` runs the per-engine `CapabilityProbe`(s) stand-alone (inject the probe seam — NO real DB), resolves the profile NAME via `resolveProfile`, derives the chosen-strategy id WITHOUT connecting, builds a `DoctorView`, and returns the `formatDoctor` output. NON-throwing: an unrecognized-environment probe (no profile match) → a report noting the unrecognized environment (shape sample only), NO exception. EXACT-set: a connected-capable fixture → the report lists methods + chosen strategy + profile; an unrecognized fixture → the report contains an "unrecognized"/default-profile marker and `runDoctor` resolves (does not reject). Spec: doctor on an unrecognized environment reports rather than throws. Done: `npm test commands/doctor`.
- [ ] 5.3 RED→GREEN extend `test/cli/dispatch.test.ts` + `src/cli/dispatch.ts`: add `async function handleDoctor` calling `runDoctor`, writing the report to stdout, returning `{ type: 'success' }`; register `doctor: handleDoctor` in `COMMAND_TABLE` (line ~260). Add the `doctor` line to `USAGE_TEXT` in `src/cli/cli.ts` (Commands block, line ~28-36): `doctor    Run a content-free connectivity self-test (safe to share)`. EXACT-set: `dispatch('doctor')` returns `{ type: 'handler' }`; `USAGE_TEXT` contains the `doctor` entry. Spec: there SHALL be a `dbgraph doctor` diagnostic command. Done: `npm test dispatch` + the USAGE assertion.
- [ ] 5.4 GATE — `npx tsc --noEmit` · `npm run lint` 0/0 · `npm test` incl. `security-scan.test.ts`. The leak assertion (5.1) + the content-free + non-throwing tests (5.2) are green. Done: all gates green.

## Batch 6: Anonymized F-1..F-9 fixtures + OPT-IN sqlcmd CI lane + E8 docs + final boundary/leak/read-only sweep + lint

> Satisfies `connectivity` ADDED "Regression fixtures and an opt-in sqlcmd CI lane cover the transport" (all three
> scenarios). US-044. Plus the docs obligation (apply owns): create `docs/stories/08-resilient-connectivity.md`
> (epic E8, US-039..044 — content finalized in the spec phase) + the README epic-table row + the count bumps.
> The opt-in lane MUST NEVER block the unit matrix (install-fail → skip-with-notice; mirrors L-003).

- [ ] 6.1 GREEN `test/fixtures/mssql/connectivity/F-1.json` .. `F-9.json` (mirror the SHIPPED `test/fixtures/mssql/rows/` layout): anonymized SHAPE-ONLY captures of the F-1..F-9 environments (variant/version, flag combos, chunk size, header flag, encoding) — NO schema name, object identifier, or proc source text. Include the F-4/F-6 2033-char no-header chunked buffer, the F-5 non-UTF-8 codepage buffer, and the F-9 `sql_variant` row that Batch 4's `reassembleForJson` golden consumes. EXACT-set: a fixture-content test asserts each file `not.toContain` any identifier/proc text (a small fixed deny-list) AND captures the required shape fields. Spec: Anonymized F-1..F-9 fixtures exist and are content-free. Done: files exist, valid JSON; content-free assertion green. (If Batch 4 inlined minimal buffers, REPOINT `json-rows-reassembly.test.ts` at these fixtures now and re-prove byte-identical.)
- [ ] 6.2 GREEN `.github/workflows/ci.yml` (or a new `sqlcmd-lane.yml`): KEEP the existing `test` + `mssql-integration` + `pg-integration` + `mysql-integration` jobs UNCHANGED; ADD a separate OPT-IN `sqlcmd-transport` job (gated by a workflow_dispatch / an explicit env flag — NOT on the default unit path; `needs: []`; never required by the unit matrix) that installs a PINNED `sqlcmd` and runs ONLY the sqlcmd-transport subset (flag combos, chunking, encoding). On install failure the step MUST `skip-with-notice` (echo a notice + exit 0 for that job) — NOT turn the matrix red. EXACT-set: the unit matrix job does NOT list `sqlcmd-transport` in `needs`; the lane's install step has an explicit skip-on-failure guard. Spec: Opt-in sqlcmd lane exercises the transport without blocking the unit matrix; A flaky sqlcmd install skips with notice rather than failing. Done: YAML valid; unit matrix independent of the opt-in lane.
- [ ] 6.3 GREEN `docs/stories/08-resilient-connectivity.md`: the E8 epic (US-039..044) with the content the spec phase finalized — each story's acceptance criteria, Phase 8.5, Status `☐ pending`, dependencies per the proposal (US-039 ← US-027/028/029; US-040/041/043 ← US-039; US-042 ← US-040; US-044 ← US-034/040). Spec: New epic E8 (US-039..044) added to docs/stories/. Done: file present and reviewable.
- [ ] 6.4 GREEN `docs/stories/README.md`: change line 4 `38 stories across 7 epics.` → `44 stories across 8 epics.`; ADD the epic-table row `| E8 — Resilient connectivity | 08-resilient-connectivity.md | US-039..044 | 8.5 |`; ADD the global-status row `| E8 | 6 | 0 |`; update `Last update:` to the current date. Spec: E8 registered in README; counts bumped. Done: README reflects 8 epics / 44 stories.
- [ ] 6.5 RED→GREEN final READ-ONLY + LEAK sweep: run `test/adapters/engines/security-scan.test.ts` over `engines/**` (the new `probe.ts` + `profiles.ts` + extracted `json-rows.ts` reassembly carry NO write verb; the run-it-yourself catalog SELECTs surfaced in outcomes are SELECT-only); add/confirm a leak-scanner assertion that `formatOutcome` + `formatDoctor` rendered outputs contain NO schema/identifier/secret over the F-1..F-9 fixtures. Spec: read-only catalog SELECTs only (US-031); doctor + outcome are content-free. Done: `npm test security-scan` + the leak assertion green.
- [ ] 6.6 Final GATE + closeout: `npx tsc --noEmit` clean; `npm run lint` 0/0; `npm test` green incl. `security-scan.test.ts` over `engines/**`; the mssql goldens + gated `DBGRAPH_INTEGRATION=1 npm run test:integration` (mssql/pg/mysql lanes) BYTE-IDENTICAL; the OPT-IN `sqlcmd-transport` lane validated (does NOT block the unit matrix). ZERO new npm deps (`npm ci` clean — probing/detection stay on `node:child_process` + dynamic `import()`). Spec: ADR-004/006/007/008 intact; native-tedious + pg + mysql + manual-dump + SQLite green; goldens byte-identical; F-7 gap closed by the opt-in lane. Done: all gates green.

## Apply Batch Grouping (one sub-agent session each)

- **Batch 1** (1.1–1.6): core types (`CapabilityProbe`/`ProbeResult`/`CliToolInfo`, `ConnectivityOption`/`ConnectivityOutcome`/`ConnectivityUnavailableError`), optional `probe?()` on the port, the pure `present/connectivity.ts` renderer, barrel exports — driver-free, NO adapters/CLI, NO DB.
- **Batch 2** (2.1–2.6): per-engine `probe.ts` for mssql/pg/mysql/sqlite (driver-present via injected `import()`; cross-platform PATH scan; non-throwing) — pure/seamed, NO DB.
- **Batch 3** (3.1–3.8): the engine-agnostic `buildConnectivityOutcome` helper, pg/mysql factory rewiring (driver-absent/connect-fail → outcome, happy path untouched), mssql `selectStrategy` exhaustion → outcome, `cli.ts` catch render, `exhaustion.ts` shim, the ENGINE-AGNOSTIC PARITY suite — pure/seamed, NO DB; re-prove happy paths.
- **Batch 4** (4.1–4.4): `profiles.ts` registry + seeded legacy-15.x, extract `reassembleForJson` to `json-rows.ts`, `sqlcmd.strategy.probe()` + profile-threaded flags — mssql BYTE-IDENTICAL gate (goldens + gated integration).
- **Batch 5** (5.1–5.4): the pure content-free `present/doctor.ts`, `runDoctor`, `handleDoctor` + `COMMAND_TABLE` + USAGE, the leak assertion — seamed, NO DB.
- **Batch 6** (6.1–6.6): anonymized F-1..F-9 fixtures, the OPT-IN sqlcmd CI lane, E8 docs + README reconciliation, the final read-only/leak sweep + lint + byte-identical closeout.

### Dependency bottlenecks

- **Batch 1 gates EVERYTHING.** `ConnectivityOutcome`/`ConnectivityUnavailableError`/`formatOutcome` and the `CapabilityProbe`/`ProbeResult` core types are consumed by EVERY later batch (probes in B2, factory/registry wiring in B3, `resolveProfile(probe)` in B4, `DoctorView` in B5). The barrel exports (1.5) must land before any adapter/CLI imports them.
- **Batch 2 (per-engine `probe.ts`) gates Batch 4 and Batch 5.** `resolveProfile(probe)` (4.1) and `sqlcmd.strategy.probe()` (4.3) consume `MssqlCapabilityProbe`; `runDoctor` (5.2) runs the probes stand-alone. The probe SHAPE must be stable before profile resolution and doctor.
- **3.1 (`buildConnectivityOutcome`) gates 3.2/3.3/3.4** — both factories AND the mssql registry call the single engine-agnostic builder, which is what makes the parity suite (3.5) a PROOF rather than three hand-rolled shapes. Splitting the builder per engine would re-litigate the resolved engine-agnostic decision — do NOT.
- **3.6 (`cli.ts` render) + 3.7 (`exhaustion.ts` shim) depend on 1.3 (`formatOutcome`)** — the CLI boundary and the legacy shim both delegate to the single core renderer; they MUST NOT each implement option formatting.
- **4.2 (extract `reassembleForJson`) is the determinism-sensitive move.** It depends on 4.1 (the profile shape it takes) and gates 4.3 (the strategy threads the profile into the extracted reassembler). The mssql goldens (4.4) are the BYTE-IDENTICAL proof — a golden drift is a hard STOP, identical to 8b's `_shared` promotion gate. The reassembly logic lives TODAY in `sqlcmd.strategy.ts` (NOT `json-rows.ts`); `parseJsonRows` is UNCHANGED.
- **Batch 6 fixtures (6.1) may be pulled EARLIER if Batch 4 prefers real F-4/F-6/F-9 fixtures over inline buffers** — if so, 6.1's content-free assertion still runs in Batch 6, but `json-rows-reassembly.test.ts` (4.2) points at them and the byte-identical gate (4.4) re-proves. The OPT-IN CI lane (6.2) needs the sqlcmd-transport subset to exist (the 4.x sqlcmd tests) before it can run them.

## Definition of Done (tied to the proposal's Success Criteria)

- [ ] An engine-agnostic pre-run probe (`CapabilityProbe`) detects available methods (native driver via `import()` presence, CLI on PATH cross-platform, ODBC) WITHOUT raising; selection uses the result; for mssql it also detects the sqlcmd variant+version and the real output shape/encoding via a tiny FOR JSON round-trip. — Batches 1, 2, 4
- [ ] On ANY engine, when no method connects, a typed NON-blocking `ConnectivityOutcome` presents ≥3 options (run-it-yourself with the exact catalog SELECTs to paste; consented install; manual-dump import) — pg/mysql driver-absent AND mssql strategy-exhaustion proven by the PARITY suite to yield the SAME option set; NO raw exception reaches the user (rendered at the `cli.ts` boundary via `formatOutcome`, never a stack trace). — Batches 1, 3
- [ ] mssql variant/version flag/format/encoding profiles live in an EXTENSIBLE registry (`profiles.ts`); adding an environment = one data row (no code patch); the measured legacy-15.x profile is a registry entry, not hard-coded; an unrecognized probe → conservative default, no crash. — Batch 4
- [ ] Chunked / no-header / padded / encoded sqlcmd output reassembles correctly across profiles via the extracted `reassembleForJson` (never `.trim()` at chunk boundaries; correct codepage decode — F-4/F-5/L-012); `sql_variant`→JSON coerces (F-9); malformed output → typed actionable error, not a `JSON.parse` stack. — Batch 4
- [ ] `dbgraph doctor` emits a CONTENT-FREE report (capability + chosen strategy + profile, shape sample only); a test asserts NO schema, object name, or proc source appears; an unrecognized environment reports rather than throws. — Batch 5
- [ ] Anonymized F-1..F-9 fixtures exist (content-free) AND an OPT-IN CI lane installs `sqlcmd` exercising the transport (flag combos, chunking, encoding) — F-7 gap closed; the lane never blocks the unit matrix (install-fail → skip-with-notice). — Batch 6
- [ ] The run-it-yourself option's queries are the EXACT shipped read-only catalog SELECTs surfaced as paste-able strings, asserted write-verb-free; the engines write-verb scanner stays green over the new `probe.ts`/`profiles.ts`/extracted `json-rows.ts`. — Batches 3, 6
- [ ] The happy-connect branch of every factory is UNCHANGED (the new path is reached ONLY on driver-absent/connect-fail/exhaustion); the SHIPPED `SchemaAdapter` return type and its success tests pass byte-for-byte. — Batch 3
- [ ] ZERO new npm dependencies; ADR-004 (core driver-free; `probe()` on the port imports no driver/`child_process`) / ADR-006/007 / ADR-008 intact; native-tedious + pg + mysql + manual-dump + SQLite remain green; mssql goldens byte-identical. — Batches 1, 4, 6
- [ ] `cli/format/exhaustion.ts` is a thin shim delegating to core `formatOutcome` (the existing exhaustion test stays green against the new ≥3-option output). — Batch 3
- [ ] New epic E8 (US-039..044) added as `docs/stories/08-resilient-connectivity.md`, registered in `docs/stories/README.md` with the count bump (38/7 → 44/8), Status pending, Phase 8.5. — Batch 6
